// This file was autogenerated by some hot garbage in the `uniffi-bindgen-react-native` crate.
// Trust me, you don't want to mess with it!
#include "swarmdrop_mobile_core.hpp"

#include "UniffiJsiTypes.h"
#include <stdexcept>
#include <map>
#include <utility>
#include <iostream>
#include <thread>

namespace react = facebook::react;
namespace jsi = facebook::jsi;

// Calling into Rust.
extern "C" {
    typedef void
    (*UniffiRustFutureContinuationCallback)(
    uint64_t data,
    int8_t poll_result
    );
    typedef void
    (*UniffiForeignFutureDroppedCallback)(
    uint64_t handle
    );
    typedef void
    (*UniffiCallbackInterfaceFree)(
    uint64_t handle
    );
    typedef uint64_t
    (*UniffiCallbackInterfaceClone)(
    uint64_t handle
    );typedef struct UniffiForeignFutureDroppedCallbackStruct {
        uint64_t handle;
        UniffiForeignFutureDroppedCallback free;
    } UniffiForeignFutureDroppedCallbackStruct;typedef struct UniffiForeignFutureResultU8 {
        uint8_t return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultU8;
    typedef void
    (*UniffiForeignFutureCompleteU8)(
    uint64_t callback_data,
    UniffiForeignFutureResultU8 result
    );typedef struct UniffiForeignFutureResultI8 {
        int8_t return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultI8;
    typedef void
    (*UniffiForeignFutureCompleteI8)(
    uint64_t callback_data,
    UniffiForeignFutureResultI8 result
    );typedef struct UniffiForeignFutureResultU16 {
        uint16_t return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultU16;
    typedef void
    (*UniffiForeignFutureCompleteU16)(
    uint64_t callback_data,
    UniffiForeignFutureResultU16 result
    );typedef struct UniffiForeignFutureResultI16 {
        int16_t return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultI16;
    typedef void
    (*UniffiForeignFutureCompleteI16)(
    uint64_t callback_data,
    UniffiForeignFutureResultI16 result
    );typedef struct UniffiForeignFutureResultU32 {
        uint32_t return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultU32;
    typedef void
    (*UniffiForeignFutureCompleteU32)(
    uint64_t callback_data,
    UniffiForeignFutureResultU32 result
    );typedef struct UniffiForeignFutureResultI32 {
        int32_t return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultI32;
    typedef void
    (*UniffiForeignFutureCompleteI32)(
    uint64_t callback_data,
    UniffiForeignFutureResultI32 result
    );typedef struct UniffiForeignFutureResultU64 {
        uint64_t return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultU64;
    typedef void
    (*UniffiForeignFutureCompleteU64)(
    uint64_t callback_data,
    UniffiForeignFutureResultU64 result
    );typedef struct UniffiForeignFutureResultI64 {
        int64_t return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultI64;
    typedef void
    (*UniffiForeignFutureCompleteI64)(
    uint64_t callback_data,
    UniffiForeignFutureResultI64 result
    );typedef struct UniffiForeignFutureResultF32 {
        float return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultF32;
    typedef void
    (*UniffiForeignFutureCompleteF32)(
    uint64_t callback_data,
    UniffiForeignFutureResultF32 result
    );typedef struct UniffiForeignFutureResultF64 {
        double return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultF64;
    typedef void
    (*UniffiForeignFutureCompleteF64)(
    uint64_t callback_data,
    UniffiForeignFutureResultF64 result
    );typedef struct UniffiForeignFutureResultRustBuffer {
        RustBuffer return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultRustBuffer;
    typedef void
    (*UniffiForeignFutureCompleteRustBuffer)(
    uint64_t callback_data,
    UniffiForeignFutureResultRustBuffer result
    );typedef struct UniffiForeignFutureResultVoid {
        RustCallStatus call_status;
    } UniffiForeignFutureResultVoid;
    typedef void
    (*UniffiForeignFutureCompleteVoid)(
    uint64_t callback_data,
    UniffiForeignFutureResultVoid result
    );
    typedef void
    (*UniffiCallbackInterfaceForeignEventBusMethod0)(
    uint64_t uniffi_handle,
    RustBuffer event,
    void * uniffi_out_return, RustCallStatus* rust_call_status
    );
    typedef void
    (*UniffiCallbackInterfaceForeignFileAccessMethod0)(
    uint64_t uniffi_handle,
    RustBuffer source_id,
    UniffiForeignFutureCompleteRustBuffer uniffi_future_callback,
    uint64_t uniffi_callback_data,
    UniffiForeignFutureDroppedCallbackStruct * uniffi_out_dropped_callback
    );
    typedef void
    (*UniffiCallbackInterfaceForeignFileAccessMethod1)(
    uint64_t uniffi_handle,
    RustBuffer source_id,
    uint64_t offset,
    uint64_t length,
    UniffiForeignFutureCompleteRustBuffer uniffi_future_callback,
    uint64_t uniffi_callback_data,
    UniffiForeignFutureDroppedCallbackStruct * uniffi_out_dropped_callback
    );
    typedef void
    (*UniffiCallbackInterfaceForeignFileAccessMethod2)(
    uint64_t uniffi_handle,
    RustBuffer metadata,
    UniffiForeignFutureCompleteRustBuffer uniffi_future_callback,
    uint64_t uniffi_callback_data,
    UniffiForeignFutureDroppedCallbackStruct * uniffi_out_dropped_callback
    );
    typedef void
    (*UniffiCallbackInterfaceForeignFileAccessMethod3)(
    uint64_t uniffi_handle,
    RustBuffer metadata,
    UniffiForeignFutureCompleteRustBuffer uniffi_future_callback,
    uint64_t uniffi_callback_data,
    UniffiForeignFutureDroppedCallbackStruct * uniffi_out_dropped_callback
    );
    typedef void
    (*UniffiCallbackInterfaceForeignFileAccessMethod4)(
    uint64_t uniffi_handle,
    RustBuffer sink_id,
    uint64_t offset,
    RustBuffer data,
    UniffiForeignFutureCompleteVoid uniffi_future_callback,
    uint64_t uniffi_callback_data,
    UniffiForeignFutureDroppedCallbackStruct * uniffi_out_dropped_callback
    );
    typedef void
    (*UniffiCallbackInterfaceForeignFileAccessMethod5)(
    uint64_t uniffi_handle,
    RustBuffer sink_id,
    UniffiForeignFutureCompleteVoid uniffi_future_callback,
    uint64_t uniffi_callback_data,
    UniffiForeignFutureDroppedCallbackStruct * uniffi_out_dropped_callback
    );
    typedef void
    (*UniffiCallbackInterfaceForeignFileAccessMethod6)(
    uint64_t uniffi_handle,
    RustBuffer sink_id,
    UniffiForeignFutureCompleteVoid uniffi_future_callback,
    uint64_t uniffi_callback_data,
    UniffiForeignFutureDroppedCallbackStruct * uniffi_out_dropped_callback
    );
    typedef void
    (*UniffiCallbackInterfaceForeignKeychainProviderMethod0)(
    uint64_t uniffi_handle,
    UniffiForeignFutureCompleteRustBuffer uniffi_future_callback,
    uint64_t uniffi_callback_data,
    UniffiForeignFutureDroppedCallbackStruct * uniffi_out_dropped_callback
    );
    typedef void
    (*UniffiCallbackInterfaceForeignKeychainProviderMethod1)(
    uint64_t uniffi_handle,
    RustBuffer keypair,
    UniffiForeignFutureCompleteVoid uniffi_future_callback,
    uint64_t uniffi_callback_data,
    UniffiForeignFutureDroppedCallbackStruct * uniffi_out_dropped_callback
    );
    typedef void
    (*UniffiCallbackInterfaceForeignKeychainProviderMethod2)(
    uint64_t uniffi_handle,
    UniffiForeignFutureCompleteVoid uniffi_future_callback,
    uint64_t uniffi_callback_data,
    UniffiForeignFutureDroppedCallbackStruct * uniffi_out_dropped_callback
    );
    typedef void
    (*UniffiCallbackInterfaceForeignKeychainProviderMethod3)(
    uint64_t uniffi_handle,
    UniffiForeignFutureCompleteRustBuffer uniffi_future_callback,
    uint64_t uniffi_callback_data,
    UniffiForeignFutureDroppedCallbackStruct * uniffi_out_dropped_callback
    );
    typedef void
    (*UniffiCallbackInterfaceForeignKeychainProviderMethod4)(
    uint64_t uniffi_handle,
    RustBuffer devices_json,
    UniffiForeignFutureCompleteVoid uniffi_future_callback,
    uint64_t uniffi_callback_data,
    UniffiForeignFutureDroppedCallbackStruct * uniffi_out_dropped_callback
    );typedef struct UniffiVTableCallbackInterfaceForeignEventBus {
        UniffiCallbackInterfaceFree uniffi_free;
        UniffiCallbackInterfaceClone uniffi_clone;
        UniffiCallbackInterfaceForeignEventBusMethod0 emit;
    } UniffiVTableCallbackInterfaceForeignEventBus;typedef struct UniffiVTableCallbackInterfaceForeignFileAccess {
        UniffiCallbackInterfaceFree uniffi_free;
        UniffiCallbackInterfaceClone uniffi_clone;
        UniffiCallbackInterfaceForeignFileAccessMethod0 source_metadata;
        UniffiCallbackInterfaceForeignFileAccessMethod1 read_source_chunk;
        UniffiCallbackInterfaceForeignFileAccessMethod2 create_sink;
        UniffiCallbackInterfaceForeignFileAccessMethod3 open_or_create_sink;
        UniffiCallbackInterfaceForeignFileAccessMethod4 write_sink_chunk;
        UniffiCallbackInterfaceForeignFileAccessMethod5 finalize_sink;
        UniffiCallbackInterfaceForeignFileAccessMethod6 cleanup_sink;
    } UniffiVTableCallbackInterfaceForeignFileAccess;typedef struct UniffiVTableCallbackInterfaceForeignKeychainProvider {
        UniffiCallbackInterfaceFree uniffi_free;
        UniffiCallbackInterfaceClone uniffi_clone;
        UniffiCallbackInterfaceForeignKeychainProviderMethod0 load_identity;
        UniffiCallbackInterfaceForeignKeychainProviderMethod1 save_identity;
        UniffiCallbackInterfaceForeignKeychainProviderMethod2 delete_identity;
        UniffiCallbackInterfaceForeignKeychainProviderMethod3 load_paired_devices_json;
        UniffiCallbackInterfaceForeignKeychainProviderMethod4 save_paired_devices_json;
    } UniffiVTableCallbackInterfaceForeignKeychainProvider;
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_clone_mobilecore(
        /*handle*/ uint64_t handle,
        RustCallStatus *uniffi_out_err
    );
    void uniffi_swarmdrop_mobile_core_fn_free_mobilecore(
        /*handle*/ uint64_t handle,
        RustCallStatus *uniffi_out_err
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_constructor_mobilecore_new(
        /*handle*/ uint64_t keychain,
        /*handle*/ uint64_t event_bus,
        /*handle*/ uint64_t file_access,
        RustBuffer data_dir,
        RustCallStatus *uniffi_out_err
    );
    RustBuffer uniffi_swarmdrop_mobile_core_fn_method_mobilecore_take_last_panic(
        /*handle*/ uint64_t ptr,
        RustCallStatus *uniffi_out_err
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_list_devices(
        /*handle*/ uint64_t ptr,
        RustBuffer filter
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_list_paired_devices(
        /*handle*/ uint64_t ptr
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_clear_transfer_activity(
        /*handle*/ uint64_t ptr
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_delete_transfer_record(
        /*handle*/ uint64_t ptr,
        RustBuffer session_id
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_get_transfer_projection(
        /*handle*/ uint64_t ptr,
        RustBuffer session_id
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_get_transfer_projections(
        /*handle*/ uint64_t ptr
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_resume_transfer(
        /*handle*/ uint64_t ptr,
        RustBuffer session_id
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_initialize_identity(
        /*handle*/ uint64_t ptr
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_archive_inbox_item(
        /*handle*/ uint64_t ptr,
        RustBuffer item_id,
        int8_t archived
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_delete_inbox_item_record(
        /*handle*/ uint64_t ptr,
        RustBuffer item_id
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_get_inbox_item(
        /*handle*/ uint64_t ptr,
        RustBuffer item_id
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_list_inbox_items(
        /*handle*/ uint64_t ptr,
        int8_t include_archived
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_mark_inbox_file_missing(
        /*handle*/ uint64_t ptr,
        RustBuffer item_id,
        uint32_t file_id,
        int8_t missing
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_mark_inbox_item_opened(
        /*handle*/ uint64_t ptr,
        RustBuffer item_id
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_repair_missing_inbox_items(
        /*handle*/ uint64_t ptr
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_network_status(
        /*handle*/ uint64_t ptr
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_shutdown_node(
        /*handle*/ uint64_t ptr
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_start_node(
        /*handle*/ uint64_t ptr,
        RustBuffer device_name,
        RustBuffer custom_bootstrap_nodes
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_generate_pairing_code(
        /*handle*/ uint64_t ptr,
        uint64_t expires_in_secs
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_lookup_device_by_code(
        /*handle*/ uint64_t ptr,
        RustBuffer code
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_request_pairing(
        /*handle*/ uint64_t ptr,
        RustBuffer peer_id,
        RustBuffer code,
        RustBuffer addrs
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_respond_pairing_request(
        /*handle*/ uint64_t ptr,
        uint64_t pending_id,
        RustBuffer code,
        int8_t accept
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_accept_receive(
        /*handle*/ uint64_t ptr,
        RustBuffer session_id,
        RustBuffer save_location_uri
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_cancel_transfer(
        /*handle*/ uint64_t ptr,
        RustBuffer session_id
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_pause_transfer(
        /*handle*/ uint64_t ptr,
        RustBuffer session_id
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_prepare_send(
        /*handle*/ uint64_t ptr,
        RustBuffer files
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_reject_receive(
        /*handle*/ uint64_t ptr,
        RustBuffer session_id
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_mobilecore_send_prepared(
        /*handle*/ uint64_t ptr,
        RustBuffer prepared_id,
        RustBuffer peer_id,
        RustBuffer peer_name,
        RustBuffer file_ids
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_clone_foreigneventbus(
        /*handle*/ uint64_t handle,
        RustCallStatus *uniffi_out_err
    );
    void uniffi_swarmdrop_mobile_core_fn_free_foreigneventbus(
        /*handle*/ uint64_t handle,
        RustCallStatus *uniffi_out_err
    );
    void uniffi_swarmdrop_mobile_core_fn_init_callback_vtable_foreigneventbus(
        UniffiVTableCallbackInterfaceForeignEventBus * vtable
    );
    void uniffi_swarmdrop_mobile_core_fn_method_foreigneventbus_emit(
        /*handle*/ uint64_t ptr,
        RustBuffer event,
        RustCallStatus *uniffi_out_err
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_clone_foreignfileaccess(
        /*handle*/ uint64_t handle,
        RustCallStatus *uniffi_out_err
    );
    void uniffi_swarmdrop_mobile_core_fn_free_foreignfileaccess(
        /*handle*/ uint64_t handle,
        RustCallStatus *uniffi_out_err
    );
    void uniffi_swarmdrop_mobile_core_fn_init_callback_vtable_foreignfileaccess(
        UniffiVTableCallbackInterfaceForeignFileAccess * vtable
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_source_metadata(
        /*handle*/ uint64_t ptr,
        RustBuffer source_id
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_read_source_chunk(
        /*handle*/ uint64_t ptr,
        RustBuffer source_id,
        uint64_t offset,
        uint64_t length
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_create_sink(
        /*handle*/ uint64_t ptr,
        RustBuffer metadata
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_open_or_create_sink(
        /*handle*/ uint64_t ptr,
        RustBuffer metadata
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_write_sink_chunk(
        /*handle*/ uint64_t ptr,
        RustBuffer sink_id,
        uint64_t offset,
        RustBuffer data
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_finalize_sink(
        /*handle*/ uint64_t ptr,
        RustBuffer sink_id
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_cleanup_sink(
        /*handle*/ uint64_t ptr,
        RustBuffer sink_id
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_clone_foreignkeychainprovider(
        /*handle*/ uint64_t handle,
        RustCallStatus *uniffi_out_err
    );
    void uniffi_swarmdrop_mobile_core_fn_free_foreignkeychainprovider(
        /*handle*/ uint64_t handle,
        RustCallStatus *uniffi_out_err
    );
    void uniffi_swarmdrop_mobile_core_fn_init_callback_vtable_foreignkeychainprovider(
        UniffiVTableCallbackInterfaceForeignKeychainProvider * vtable
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_load_identity(
        /*handle*/ uint64_t ptr
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_save_identity(
        /*handle*/ uint64_t ptr,
        RustBuffer keypair
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_delete_identity(
        /*handle*/ uint64_t ptr
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_load_paired_devices_json(
        /*handle*/ uint64_t ptr
    );
    /*handle*/ uint64_t uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_save_paired_devices_json(
        /*handle*/ uint64_t ptr,
        RustBuffer devices_json
    );
    RustBuffer ffi_swarmdrop_mobile_core_rustbuffer_alloc(
        uint64_t size,
        RustCallStatus *uniffi_out_err
    );
    RustBuffer ffi_swarmdrop_mobile_core_rustbuffer_from_bytes(
        ForeignBytes bytes,
        RustCallStatus *uniffi_out_err
    );
    void ffi_swarmdrop_mobile_core_rustbuffer_free(
        RustBuffer buf,
        RustCallStatus *uniffi_out_err
    );
    RustBuffer ffi_swarmdrop_mobile_core_rustbuffer_reserve(
        RustBuffer buf,
        uint64_t additional,
        RustCallStatus *uniffi_out_err
    );
    void ffi_swarmdrop_mobile_core_rust_future_poll_u8(
        /*handle*/ uint64_t handle,
        UniffiRustFutureContinuationCallback callback,
        /*handle*/ uint64_t callback_data
    );
    void ffi_swarmdrop_mobile_core_rust_future_cancel_u8(
        /*handle*/ uint64_t handle
    );
    void ffi_swarmdrop_mobile_core_rust_future_free_u8(
        /*handle*/ uint64_t handle
    );
    uint8_t ffi_swarmdrop_mobile_core_rust_future_complete_u8(
        /*handle*/ uint64_t handle,
        RustCallStatus *uniffi_out_err
    );
    void ffi_swarmdrop_mobile_core_rust_future_poll_i8(
        /*handle*/ uint64_t handle,
        UniffiRustFutureContinuationCallback callback,
        /*handle*/ uint64_t callback_data
    );
    void ffi_swarmdrop_mobile_core_rust_future_cancel_i8(
        /*handle*/ uint64_t handle
    );
    void ffi_swarmdrop_mobile_core_rust_future_free_i8(
        /*handle*/ uint64_t handle
    );
    int8_t ffi_swarmdrop_mobile_core_rust_future_complete_i8(
        /*handle*/ uint64_t handle,
        RustCallStatus *uniffi_out_err
    );
    void ffi_swarmdrop_mobile_core_rust_future_poll_u16(
        /*handle*/ uint64_t handle,
        UniffiRustFutureContinuationCallback callback,
        /*handle*/ uint64_t callback_data
    );
    void ffi_swarmdrop_mobile_core_rust_future_cancel_u16(
        /*handle*/ uint64_t handle
    );
    void ffi_swarmdrop_mobile_core_rust_future_free_u16(
        /*handle*/ uint64_t handle
    );
    uint16_t ffi_swarmdrop_mobile_core_rust_future_complete_u16(
        /*handle*/ uint64_t handle,
        RustCallStatus *uniffi_out_err
    );
    void ffi_swarmdrop_mobile_core_rust_future_poll_i16(
        /*handle*/ uint64_t handle,
        UniffiRustFutureContinuationCallback callback,
        /*handle*/ uint64_t callback_data
    );
    void ffi_swarmdrop_mobile_core_rust_future_cancel_i16(
        /*handle*/ uint64_t handle
    );
    void ffi_swarmdrop_mobile_core_rust_future_free_i16(
        /*handle*/ uint64_t handle
    );
    int16_t ffi_swarmdrop_mobile_core_rust_future_complete_i16(
        /*handle*/ uint64_t handle,
        RustCallStatus *uniffi_out_err
    );
    void ffi_swarmdrop_mobile_core_rust_future_poll_u32(
        /*handle*/ uint64_t handle,
        UniffiRustFutureContinuationCallback callback,
        /*handle*/ uint64_t callback_data
    );
    void ffi_swarmdrop_mobile_core_rust_future_cancel_u32(
        /*handle*/ uint64_t handle
    );
    void ffi_swarmdrop_mobile_core_rust_future_free_u32(
        /*handle*/ uint64_t handle
    );
    uint32_t ffi_swarmdrop_mobile_core_rust_future_complete_u32(
        /*handle*/ uint64_t handle,
        RustCallStatus *uniffi_out_err
    );
    void ffi_swarmdrop_mobile_core_rust_future_poll_i32(
        /*handle*/ uint64_t handle,
        UniffiRustFutureContinuationCallback callback,
        /*handle*/ uint64_t callback_data
    );
    void ffi_swarmdrop_mobile_core_rust_future_cancel_i32(
        /*handle*/ uint64_t handle
    );
    void ffi_swarmdrop_mobile_core_rust_future_free_i32(
        /*handle*/ uint64_t handle
    );
    int32_t ffi_swarmdrop_mobile_core_rust_future_complete_i32(
        /*handle*/ uint64_t handle,
        RustCallStatus *uniffi_out_err
    );
    void ffi_swarmdrop_mobile_core_rust_future_poll_u64(
        /*handle*/ uint64_t handle,
        UniffiRustFutureContinuationCallback callback,
        /*handle*/ uint64_t callback_data
    );
    void ffi_swarmdrop_mobile_core_rust_future_cancel_u64(
        /*handle*/ uint64_t handle
    );
    void ffi_swarmdrop_mobile_core_rust_future_free_u64(
        /*handle*/ uint64_t handle
    );
    uint64_t ffi_swarmdrop_mobile_core_rust_future_complete_u64(
        /*handle*/ uint64_t handle,
        RustCallStatus *uniffi_out_err
    );
    void ffi_swarmdrop_mobile_core_rust_future_poll_i64(
        /*handle*/ uint64_t handle,
        UniffiRustFutureContinuationCallback callback,
        /*handle*/ uint64_t callback_data
    );
    void ffi_swarmdrop_mobile_core_rust_future_cancel_i64(
        /*handle*/ uint64_t handle
    );
    void ffi_swarmdrop_mobile_core_rust_future_free_i64(
        /*handle*/ uint64_t handle
    );
    int64_t ffi_swarmdrop_mobile_core_rust_future_complete_i64(
        /*handle*/ uint64_t handle,
        RustCallStatus *uniffi_out_err
    );
    void ffi_swarmdrop_mobile_core_rust_future_poll_f32(
        /*handle*/ uint64_t handle,
        UniffiRustFutureContinuationCallback callback,
        /*handle*/ uint64_t callback_data
    );
    void ffi_swarmdrop_mobile_core_rust_future_cancel_f32(
        /*handle*/ uint64_t handle
    );
    void ffi_swarmdrop_mobile_core_rust_future_free_f32(
        /*handle*/ uint64_t handle
    );
    float ffi_swarmdrop_mobile_core_rust_future_complete_f32(
        /*handle*/ uint64_t handle,
        RustCallStatus *uniffi_out_err
    );
    void ffi_swarmdrop_mobile_core_rust_future_poll_f64(
        /*handle*/ uint64_t handle,
        UniffiRustFutureContinuationCallback callback,
        /*handle*/ uint64_t callback_data
    );
    void ffi_swarmdrop_mobile_core_rust_future_cancel_f64(
        /*handle*/ uint64_t handle
    );
    void ffi_swarmdrop_mobile_core_rust_future_free_f64(
        /*handle*/ uint64_t handle
    );
    double ffi_swarmdrop_mobile_core_rust_future_complete_f64(
        /*handle*/ uint64_t handle,
        RustCallStatus *uniffi_out_err
    );
    void ffi_swarmdrop_mobile_core_rust_future_poll_rust_buffer(
        /*handle*/ uint64_t handle,
        UniffiRustFutureContinuationCallback callback,
        /*handle*/ uint64_t callback_data
    );
    void ffi_swarmdrop_mobile_core_rust_future_cancel_rust_buffer(
        /*handle*/ uint64_t handle
    );
    void ffi_swarmdrop_mobile_core_rust_future_free_rust_buffer(
        /*handle*/ uint64_t handle
    );
    RustBuffer ffi_swarmdrop_mobile_core_rust_future_complete_rust_buffer(
        /*handle*/ uint64_t handle,
        RustCallStatus *uniffi_out_err
    );
    void ffi_swarmdrop_mobile_core_rust_future_poll_void(
        /*handle*/ uint64_t handle,
        UniffiRustFutureContinuationCallback callback,
        /*handle*/ uint64_t callback_data
    );
    void ffi_swarmdrop_mobile_core_rust_future_cancel_void(
        /*handle*/ uint64_t handle
    );
    void ffi_swarmdrop_mobile_core_rust_future_free_void(
        /*handle*/ uint64_t handle
    );
    void ffi_swarmdrop_mobile_core_rust_future_complete_void(
        /*handle*/ uint64_t handle,
        RustCallStatus *uniffi_out_err
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_take_last_panic(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_list_devices(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_list_paired_devices(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_clear_transfer_activity(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_delete_transfer_record(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_get_transfer_projection(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_get_transfer_projections(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_resume_transfer(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_initialize_identity(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_archive_inbox_item(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_delete_inbox_item_record(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_get_inbox_item(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_list_inbox_items(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_mark_inbox_file_missing(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_mark_inbox_item_opened(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_repair_missing_inbox_items(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_network_status(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_shutdown_node(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_start_node(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_generate_pairing_code(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_lookup_device_by_code(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_request_pairing(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_respond_pairing_request(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_accept_receive(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_cancel_transfer(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_pause_transfer(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_prepare_send(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_reject_receive(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_send_prepared(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_foreigneventbus_emit(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_source_metadata(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_read_source_chunk(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_create_sink(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_open_or_create_sink(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_write_sink_chunk(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_finalize_sink(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_cleanup_sink(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_load_identity(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_save_identity(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_delete_identity(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_load_paired_devices_json(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_save_paired_devices_json(
    );
    uint16_t uniffi_swarmdrop_mobile_core_checksum_constructor_mobilecore_new(
    );
    uint32_t ffi_swarmdrop_mobile_core_uniffi_contract_version(
    );
}


namespace uniffi::swarmdrop_mobile_core {
template <typename T> struct Bridging;

using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <typename T> struct Bridging<ReferenceHolder<T>> {
  static jsi::Value jsNew(jsi::Runtime &rt) {
    auto holder = jsi::Object(rt);
    return holder;
  }
  static T fromJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker,
                  const jsi::Value &value) {
    auto obj = value.asObject(rt);
    if (obj.hasProperty(rt, "pointee")) {
      auto pointee = obj.getProperty(rt, "pointee");
      return uniffi::swarmdrop_mobile_core::Bridging<T>::fromJs(rt, callInvoker, pointee);
    }
    throw jsi::JSError(
      rt,
      "Expected ReferenceHolder to have a pointee property. This is likely a bug in uniffi-bindgen-react-native"
    );
  }
};
} // namespace uniffi::swarmdrop_mobile_core
namespace uniffi::swarmdrop_mobile_core {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<RustBuffer> {
  static RustBuffer rustbuffer_alloc(int32_t size) {
      RustCallStatus status = { UNIFFI_CALL_STATUS_OK };
      return ffi_swarmdrop_mobile_core_rustbuffer_alloc(
          size,
          &status
      );
  }

  static void rustbuffer_free(RustBuffer buf) {
    RustCallStatus status = { UNIFFI_CALL_STATUS_OK };
    ffi_swarmdrop_mobile_core_rustbuffer_free(
        buf,
        &status
    );
  }

  static RustBuffer rustbuffer_from_bytes(ForeignBytes bytes) {
    RustCallStatus status = { UNIFFI_CALL_STATUS_OK };
    return ffi_swarmdrop_mobile_core_rustbuffer_from_bytes(
      bytes,
      &status
    );
  }

  static RustBuffer fromJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker>,
                           const jsi::Value &value) {
    try {
      auto buffer = uniffi_jsi::Bridging<jsi::ArrayBuffer>::value_to_arraybuffer(rt, value);
      auto bytes = ForeignBytes{
          .len = static_cast<int32_t>(buffer.length(rt)),
          .data = buffer.data(rt),
      };

      // This buffer is constructed from foreign bytes. Rust scaffolding copies
      // the bytes, to make the RustBuffer.
      auto buf = rustbuffer_from_bytes(bytes);
      // Once it leaves this function, the buffer is immediately passed back
      // into Rust, where it's used to deserialize into the Rust versions of the
      // arguments. At that point, the copy is destroyed.
      return buf;
    } catch (const std::logic_error &e) {
      throw jsi::JSError(rt, e.what());
    }
  }

  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker>,
                         RustBuffer buf) {
    // We need to make a copy of the bytes from Rust's memory space into
    // Javascripts memory space. We need to do this because the two languages
    // manages memory very differently: a garbage collector needs to track all
    // the memory at runtime, Rust is doing it all closer to compile time.
    uint8_t *bytes = new uint8_t[buf.len];
    std::memcpy(bytes, buf.data, buf.len);

    // Construct an ArrayBuffer with copy of the bytes from the RustBuffer.
    auto payload = std::make_shared<uniffi_jsi::CMutableBuffer>(
        uniffi_jsi::CMutableBuffer((uint8_t *)bytes, buf.len));
    auto arrayBuffer = jsi::ArrayBuffer(rt, payload);

    // Once we have a Javascript version, we no longer need the Rust version, so
    // we can call into Rust to tell it it's okay to free that memory.
    rustbuffer_free(buf);

    // Finally, return the ArrayBuffer.
    return uniffi_jsi::Bridging<jsi::ArrayBuffer>::arraybuffer_to_value(rt, arrayBuffer);;
  }
};

} // namespace uniffi::swarmdrop_mobile_core

namespace uniffi::swarmdrop_mobile_core {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<RustCallStatus> {
  static jsi::Value jsSuccess(jsi::Runtime &rt) {
    auto statusObject = jsi::Object(rt);
    statusObject.setProperty(rt, "code", jsi::Value(rt, UNIFFI_CALL_STATUS_OK));
    return statusObject;
  }
  static RustCallStatus rustSuccess(jsi::Runtime &rt) {
    return {UNIFFI_CALL_STATUS_OK};
  }
  static void copyIntoJs(jsi::Runtime &rt,
                         std::shared_ptr<CallInvoker> callInvoker,
                         const RustCallStatus status,
                         const jsi::Value &jsStatus) {
    auto statusObject = jsStatus.asObject(rt);
    if (status.error_buf.data != nullptr) {
      auto rbuf = Bridging<RustBuffer>::toJs(rt, callInvoker,
                                                         status.error_buf);
      statusObject.setProperty(rt, "errorBuf", rbuf);
    }
    if (status.code != UNIFFI_CALL_STATUS_OK) {
      auto code =
          uniffi_jsi::Bridging<uint8_t>::toJs(rt, callInvoker, status.code);
      statusObject.setProperty(rt, "code", code);
    }
  }

  static RustCallStatus fromJs(jsi::Runtime &rt,
                               std::shared_ptr<CallInvoker> invoker,
                               const jsi::Value &jsStatus) {
    RustCallStatus status;
    auto statusObject = jsStatus.asObject(rt);
    if (statusObject.hasProperty(rt, "errorBuf")) {
      auto rbuf = statusObject.getProperty(rt, "errorBuf");
      status.error_buf =
          Bridging<RustBuffer>::fromJs(rt, invoker, rbuf);
    }
    if (statusObject.hasProperty(rt, "code")) {
      auto code = statusObject.getProperty(rt, "code");
      status.code = uniffi_jsi::Bridging<uint8_t>::fromJs(rt, invoker, code);
    }
    return status;
  }

  static void copyFromJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> invoker,
                         const jsi::Value &jsStatus, RustCallStatus *status) {
    auto statusObject = jsStatus.asObject(rt);
    if (statusObject.hasProperty(rt, "errorBuf")) {
      auto rbuf = statusObject.getProperty(rt, "errorBuf");
      status->error_buf =
          Bridging<RustBuffer>::fromJs(rt, invoker, rbuf);
    }
    if (statusObject.hasProperty(rt, "code")) {
      auto code = statusObject.getProperty(rt, "code");
      status->code = uniffi_jsi::Bridging<uint8_t>::fromJs(rt, invoker, code);
    }
  }
};

} // namespace uniffi::swarmdrop_mobile_core
// In other uniffi bindings, it is assumed that the foreign language holds on
// to the vtable, which the Rust just gets a pointer to.
// Here, we need to hold on to them, but also be able to clear them at just the
// right time so we can support hot-reloading.
namespace uniffi::swarmdrop_mobile_core::registry {
    template <typename T>
    class VTableHolder {
    public:
        T vtable;
        VTableHolder(T v) : vtable(v) {}
    };

    // Mutex to bind the storage and setting of vtable together.
    // We declare it here, but the lock is taken by callers of the putTable
    // method who are also sending a pointer to Rust.
    static std::mutex vtableMutex;

    // Registry to hold all vtables so they persist even when JS objects are GC'd.
    // The only reason this exists is to prevent a dangling pointer in the
    // Rust machinery: i.e. we don't need to access or write to this registry
    // after startup.
    // Registry to hold all vtables so they persist even when JS objects are GC'd.
    // Maps string identifiers to vtable holders using type erasure
    static std::unordered_map<std::string, std::shared_ptr<void>> vtableRegistry;

    // Add a vtable to the registry with an identifier
    template <typename T>
    static T* putTable(std::string_view identifier, T vtable) {
        auto holder = std::make_shared<VTableHolder<T>>(vtable);
        // Store the raw pointer to the vtable before type erasure
        T* rawPtr = &(holder->vtable);
        // Store the holder using type erasure with the string identifier
        vtableRegistry[std::string(identifier)] = std::shared_ptr<void>(holder);
        return rawPtr;
    }

    // Clear the registry.
    //
    // Conceptually, this is called after teardown of the module (i.e. after
    // teardown of the jsi::Runtime). However, because Rust is dropping callbacks
    // because the Runtime is being torn down, we must keep the registry intact
    // until after the runtime goes away.
    //
    // Therefore, in practice we should call this when the next runtime is
    // being stood up.
    static void clearRegistry() {
        std::lock_guard<std::mutex> lock(vtableMutex);
        vtableRegistry.clear();
    }
} // namespace uniffi::swarmdrop_mobile_core::registry

// This calls into Rust.
    // Implementation of callback function calling from Rust to JS RustFutureContinuationCallback

// Callback function: uniffi::swarmdrop_mobile_core::cb::rustfuturecontinuationcallback::UniffiRustFutureContinuationCallback
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::cb::rustfuturecontinuationcallback {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, int8_t)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_data
            ,int8_t rs_pollResult) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_data = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_data);
        auto js_pollResult = uniffi_jsi::Bridging<int8_t>::toJs(rt, callInvoker, rs_pollResult);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_data, js_pollResult
            );




        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiRustFutureContinuationCallback: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback(uint64_t rs_data, int8_t rs_pollResult) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_data,
            rs_pollResult);
    }

    [[maybe_unused]] static UniffiRustFutureContinuationCallback
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::cb::rustfuturecontinuationcallback
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_data, int8_t rs_pollResult) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_data
                    , rs_pollResult](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_data
                        , rs_pollResult);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.

                callInvoker->invokeNonBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::cb::rustfuturecontinuationcallback
    // Implementation of callback function calling from Rust to JS ForeignFutureDroppedCallback

// Callback function: uniffi::swarmdrop_mobile_core::cb::foreignfuturedroppedcallback::UniffiForeignFutureDroppedCallback
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::cb::foreignfuturedroppedcallback {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_handle) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_handle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_handle);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_handle
            );




        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiForeignFutureDroppedCallback: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback(uint64_t rs_handle) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_handle);
    }

    [[maybe_unused]] static UniffiForeignFutureDroppedCallback
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::cb::foreignfuturedroppedcallback
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_handle) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_handle](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_handle);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.

                callInvoker->invokeNonBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::cb::foreignfuturedroppedcallback
    // Implementation of free callback function CallbackInterfaceFree


// Callback function: uniffi::swarmdrop_mobile_core::st::vtablecallbackinterfaceforeigneventbus::vtablecallbackinterfaceforeigneventbus::free::UniffiCallbackInterfaceFree
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::st::vtablecallbackinterfaceforeigneventbus::vtablecallbackinterfaceforeigneventbus::free {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_handle) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_handle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_handle);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_handle
            );




        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceFree: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback(uint64_t rs_handle) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_handle);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceFree
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::st::vtablecallbackinterfaceforeigneventbus::vtablecallbackinterfaceforeigneventbus::free
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_handle) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_handle](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_handle);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.

                callInvoker->invokeNonBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::st::vtablecallbackinterfaceforeigneventbus::vtablecallbackinterfaceforeigneventbus::free

// Callback function: uniffi::swarmdrop_mobile_core::st::vtablecallbackinterfaceforeignfileaccess::vtablecallbackinterfaceforeignfileaccess::free::UniffiCallbackInterfaceFree
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::st::vtablecallbackinterfaceforeignfileaccess::vtablecallbackinterfaceforeignfileaccess::free {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_handle) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_handle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_handle);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_handle
            );




        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceFree: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback(uint64_t rs_handle) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_handle);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceFree
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::st::vtablecallbackinterfaceforeignfileaccess::vtablecallbackinterfaceforeignfileaccess::free
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_handle) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_handle](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_handle);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.

                callInvoker->invokeNonBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::st::vtablecallbackinterfaceforeignfileaccess::vtablecallbackinterfaceforeignfileaccess::free

// Callback function: uniffi::swarmdrop_mobile_core::st::vtablecallbackinterfaceforeignkeychainprovider::vtablecallbackinterfaceforeignkeychainprovider::free::UniffiCallbackInterfaceFree
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::st::vtablecallbackinterfaceforeignkeychainprovider::vtablecallbackinterfaceforeignkeychainprovider::free {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_handle) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_handle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_handle);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_handle
            );




        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceFree: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback(uint64_t rs_handle) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_handle);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceFree
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::st::vtablecallbackinterfaceforeignkeychainprovider::vtablecallbackinterfaceforeignkeychainprovider::free
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_handle) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_handle](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_handle);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.

                callInvoker->invokeNonBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::st::vtablecallbackinterfaceforeignkeychainprovider::vtablecallbackinterfaceforeignkeychainprovider::free
namespace uniffi::swarmdrop_mobile_core {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureDroppedCallbackStruct> {
  static UniffiForeignFutureDroppedCallbackStruct fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureDroppedCallbackStruct");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureDroppedCallbackStruct rsObject;

    // Create the vtable from the js callbacks.
    rsObject.handle = uniffi_jsi::Bridging<uint64_t>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "handle")
      );
    rsObject.free = uniffi::swarmdrop_mobile_core::cb::foreignfuturedroppedcallback::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "free")
        );

    return rsObject;
  }
};

} // namespace uniffi::swarmdrop_mobile_core
namespace uniffi::swarmdrop_mobile_core {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultU8> {
  static UniffiForeignFutureResultU8 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultU8");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultU8 rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi_jsi::Bridging<uint8_t>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }
};

} // namespace uniffi::swarmdrop_mobile_core
    // Implementation of callback function calling from JS to Rust ForeignFutureCompleteU8,
    // passed from Rust to JS as part of async callbacks.
namespace uniffi::swarmdrop_mobile_core {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureCompleteU8> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteU8 rsCallback) {
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteU8"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteU8 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureResultU8>::fromJs(rt, callInvoker, args[1])
        );


        return jsi::Value::undefined();
  }
};
} // namespace uniffi::swarmdrop_mobile_core
namespace uniffi::swarmdrop_mobile_core {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultI8> {
  static UniffiForeignFutureResultI8 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultI8");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultI8 rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi_jsi::Bridging<int8_t>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }
};

} // namespace uniffi::swarmdrop_mobile_core
    // Implementation of callback function calling from JS to Rust ForeignFutureCompleteI8,
    // passed from Rust to JS as part of async callbacks.
namespace uniffi::swarmdrop_mobile_core {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureCompleteI8> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteI8 rsCallback) {
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteI8"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteI8 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureResultI8>::fromJs(rt, callInvoker, args[1])
        );


        return jsi::Value::undefined();
  }
};
} // namespace uniffi::swarmdrop_mobile_core
namespace uniffi::swarmdrop_mobile_core {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultU16> {
  static UniffiForeignFutureResultU16 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultU16");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultU16 rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi_jsi::Bridging<uint16_t>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }
};

} // namespace uniffi::swarmdrop_mobile_core
    // Implementation of callback function calling from JS to Rust ForeignFutureCompleteU16,
    // passed from Rust to JS as part of async callbacks.
namespace uniffi::swarmdrop_mobile_core {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureCompleteU16> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteU16 rsCallback) {
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteU16"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteU16 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureResultU16>::fromJs(rt, callInvoker, args[1])
        );


        return jsi::Value::undefined();
  }
};
} // namespace uniffi::swarmdrop_mobile_core
namespace uniffi::swarmdrop_mobile_core {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultI16> {
  static UniffiForeignFutureResultI16 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultI16");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultI16 rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi_jsi::Bridging<int16_t>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }
};

} // namespace uniffi::swarmdrop_mobile_core
    // Implementation of callback function calling from JS to Rust ForeignFutureCompleteI16,
    // passed from Rust to JS as part of async callbacks.
namespace uniffi::swarmdrop_mobile_core {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureCompleteI16> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteI16 rsCallback) {
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteI16"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteI16 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureResultI16>::fromJs(rt, callInvoker, args[1])
        );


        return jsi::Value::undefined();
  }
};
} // namespace uniffi::swarmdrop_mobile_core
namespace uniffi::swarmdrop_mobile_core {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultU32> {
  static UniffiForeignFutureResultU32 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultU32");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultU32 rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi_jsi::Bridging<uint32_t>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }
};

} // namespace uniffi::swarmdrop_mobile_core
    // Implementation of callback function calling from JS to Rust ForeignFutureCompleteU32,
    // passed from Rust to JS as part of async callbacks.
namespace uniffi::swarmdrop_mobile_core {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureCompleteU32> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteU32 rsCallback) {
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteU32"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteU32 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureResultU32>::fromJs(rt, callInvoker, args[1])
        );


        return jsi::Value::undefined();
  }
};
} // namespace uniffi::swarmdrop_mobile_core
namespace uniffi::swarmdrop_mobile_core {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultI32> {
  static UniffiForeignFutureResultI32 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultI32");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultI32 rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi_jsi::Bridging<int32_t>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }
};

} // namespace uniffi::swarmdrop_mobile_core
    // Implementation of callback function calling from JS to Rust ForeignFutureCompleteI32,
    // passed from Rust to JS as part of async callbacks.
namespace uniffi::swarmdrop_mobile_core {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureCompleteI32> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteI32 rsCallback) {
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteI32"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteI32 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureResultI32>::fromJs(rt, callInvoker, args[1])
        );


        return jsi::Value::undefined();
  }
};
} // namespace uniffi::swarmdrop_mobile_core
namespace uniffi::swarmdrop_mobile_core {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultU64> {
  static UniffiForeignFutureResultU64 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultU64");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultU64 rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi_jsi::Bridging<uint64_t>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }
};

} // namespace uniffi::swarmdrop_mobile_core
    // Implementation of callback function calling from JS to Rust ForeignFutureCompleteU64,
    // passed from Rust to JS as part of async callbacks.
namespace uniffi::swarmdrop_mobile_core {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureCompleteU64> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteU64 rsCallback) {
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteU64"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteU64 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureResultU64>::fromJs(rt, callInvoker, args[1])
        );


        return jsi::Value::undefined();
  }
};
} // namespace uniffi::swarmdrop_mobile_core
namespace uniffi::swarmdrop_mobile_core {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultI64> {
  static UniffiForeignFutureResultI64 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultI64");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultI64 rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi_jsi::Bridging<int64_t>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }
};

} // namespace uniffi::swarmdrop_mobile_core
    // Implementation of callback function calling from JS to Rust ForeignFutureCompleteI64,
    // passed from Rust to JS as part of async callbacks.
namespace uniffi::swarmdrop_mobile_core {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureCompleteI64> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteI64 rsCallback) {
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteI64"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteI64 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureResultI64>::fromJs(rt, callInvoker, args[1])
        );


        return jsi::Value::undefined();
  }
};
} // namespace uniffi::swarmdrop_mobile_core
namespace uniffi::swarmdrop_mobile_core {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultF32> {
  static UniffiForeignFutureResultF32 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultF32");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultF32 rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi_jsi::Bridging<float>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }
};

} // namespace uniffi::swarmdrop_mobile_core
    // Implementation of callback function calling from JS to Rust ForeignFutureCompleteF32,
    // passed from Rust to JS as part of async callbacks.
namespace uniffi::swarmdrop_mobile_core {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureCompleteF32> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteF32 rsCallback) {
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteF32"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteF32 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureResultF32>::fromJs(rt, callInvoker, args[1])
        );


        return jsi::Value::undefined();
  }
};
} // namespace uniffi::swarmdrop_mobile_core
namespace uniffi::swarmdrop_mobile_core {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultF64> {
  static UniffiForeignFutureResultF64 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultF64");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultF64 rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi_jsi::Bridging<double>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }
};

} // namespace uniffi::swarmdrop_mobile_core
    // Implementation of callback function calling from JS to Rust ForeignFutureCompleteF64,
    // passed from Rust to JS as part of async callbacks.
namespace uniffi::swarmdrop_mobile_core {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureCompleteF64> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteF64 rsCallback) {
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteF64"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteF64 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureResultF64>::fromJs(rt, callInvoker, args[1])
        );


        return jsi::Value::undefined();
  }
};
} // namespace uniffi::swarmdrop_mobile_core
namespace uniffi::swarmdrop_mobile_core {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultRustBuffer> {
  static UniffiForeignFutureResultRustBuffer fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultRustBuffer");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultRustBuffer rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }
};

} // namespace uniffi::swarmdrop_mobile_core
    // Implementation of callback function calling from JS to Rust ForeignFutureCompleteRustBuffer,
    // passed from Rust to JS as part of async callbacks.
namespace uniffi::swarmdrop_mobile_core {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureCompleteRustBuffer> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteRustBuffer rsCallback) {
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteRustBuffer"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteRustBuffer func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureResultRustBuffer>::fromJs(rt, callInvoker, args[1])
        );


        return jsi::Value::undefined();
  }
};
} // namespace uniffi::swarmdrop_mobile_core
namespace uniffi::swarmdrop_mobile_core {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultVoid> {
  static UniffiForeignFutureResultVoid fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultVoid");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultVoid rsObject;

    // Create the vtable from the js callbacks.
    rsObject.call_status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }
};

} // namespace uniffi::swarmdrop_mobile_core
    // Implementation of callback function calling from JS to Rust ForeignFutureCompleteVoid,
    // passed from Rust to JS as part of async callbacks.
namespace uniffi::swarmdrop_mobile_core {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureCompleteVoid> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteVoid rsCallback) {
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteVoid"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteVoid func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureResultVoid>::fromJs(rt, callInvoker, args[1])
        );


        return jsi::Value::undefined();
  }
};
} // namespace uniffi::swarmdrop_mobile_core
    // Implementation of CallbackInterfaceClone for vtable field uniffi_clone in VTableCallbackInterfaceForeignEventBus


// Callback function: uniffi::swarmdrop_mobile_core::cb::callbackinterfaceclone::vtablecallbackinterfaceforeigneventbus::UniffiCallbackInterfaceClone
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceclone::vtablecallbackinterfaceforeigneventbus {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, uint64_t*)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_handle
            , uint64_t* uniffi_direct_return) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_handle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_handle);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_handle
            );




            // Write the direct return value back to the caller.
            if (uniffi_direct_return != nullptr) {
                *uniffi_direct_return = uniffi_jsi::Bridging<uint64_t>::fromJs(
                    rt, callInvoker, uniffiResult
                );
            }
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceClone: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static uint64_t callback(uint64_t rs_handle) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return 0;
        }
        uint64_t uniffi_result = 0;

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_handle,
            &uniffi_result);
        return uniffi_result;
    }

    [[maybe_unused]] static UniffiCallbackInterfaceClone
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::cb::callbackinterfaceclone::vtablecallbackinterfaceforeigneventbus
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_handle, uint64_t* uniffi_direct_return) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_handle, uniffi_direct_return](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_handle, uniffi_direct_return);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceclone::vtablecallbackinterfaceforeigneventbus
    // Implementation of CallbackInterfaceForeignEventBusMethod0 for vtable field emit in VTableCallbackInterfaceForeignEventBus


// Callback function: uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeigneventbusmethod0::vtablecallbackinterfaceforeigneventbus::UniffiCallbackInterfaceForeignEventBusMethod0
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeigneventbusmethod0::vtablecallbackinterfaceforeigneventbus {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_event
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_event = uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_event);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_event
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }


        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceForeignEventBusMethod0: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback(uint64_t rs_uniffiHandle, RustBuffer rs_event, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_uniffiHandle,
            rs_event,
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceForeignEventBusMethod0
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeigneventbusmethod0::vtablecallbackinterfaceforeigneventbus
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_event, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_event
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_event
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeigneventbusmethod0::vtablecallbackinterfaceforeigneventbus
namespace uniffi::swarmdrop_mobile_core {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiVTableCallbackInterfaceForeignEventBus> {
  static UniffiVTableCallbackInterfaceForeignEventBus fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiVTableCallbackInterfaceForeignEventBus");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiVTableCallbackInterfaceForeignEventBus rsObject;

    // Create the vtable from the js callbacks.
    rsObject.uniffi_free = uniffi::swarmdrop_mobile_core::st::vtablecallbackinterfaceforeigneventbus::vtablecallbackinterfaceforeigneventbus::free::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "uniffiFree")
        );
    rsObject.uniffi_clone = uniffi::swarmdrop_mobile_core::cb::callbackinterfaceclone::vtablecallbackinterfaceforeigneventbus::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "uniffiClone")
        );
    rsObject.emit = uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeigneventbusmethod0::vtablecallbackinterfaceforeigneventbus::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "emit")
        );

    return rsObject;
  }
};

} // namespace uniffi::swarmdrop_mobile_core
    // Implementation of CallbackInterfaceClone for vtable field uniffi_clone in VTableCallbackInterfaceForeignFileAccess


// Callback function: uniffi::swarmdrop_mobile_core::cb::callbackinterfaceclone::vtablecallbackinterfaceforeignfileaccess::UniffiCallbackInterfaceClone
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceclone::vtablecallbackinterfaceforeignfileaccess {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, uint64_t*)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_handle
            , uint64_t* uniffi_direct_return) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_handle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_handle);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_handle
            );




            // Write the direct return value back to the caller.
            if (uniffi_direct_return != nullptr) {
                *uniffi_direct_return = uniffi_jsi::Bridging<uint64_t>::fromJs(
                    rt, callInvoker, uniffiResult
                );
            }
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceClone: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static uint64_t callback(uint64_t rs_handle) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return 0;
        }
        uint64_t uniffi_result = 0;

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_handle,
            &uniffi_result);
        return uniffi_result;
    }

    [[maybe_unused]] static UniffiCallbackInterfaceClone
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::cb::callbackinterfaceclone::vtablecallbackinterfaceforeignfileaccess
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_handle, uint64_t* uniffi_direct_return) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_handle, uniffi_direct_return](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_handle, uniffi_direct_return);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceclone::vtablecallbackinterfaceforeignfileaccess
    // Implementation of CallbackInterfaceForeignFileAccessMethod0 for vtable field source_metadata in VTableCallbackInterfaceForeignFileAccess


// Callback function: uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod0::vtablecallbackinterfaceforeignfileaccess::UniffiCallbackInterfaceForeignFileAccessMethod0
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod0::vtablecallbackinterfaceforeignfileaccess {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, UniffiForeignFutureCompleteRustBuffer, uint64_t, UniffiForeignFutureDroppedCallbackStruct *)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_sourceId
            ,UniffiForeignFutureCompleteRustBuffer rs_uniffiFutureCallback
            ,uint64_t rs_uniffiCallbackData
            ,UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_sourceId = uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_sourceId);
        auto js_uniffiFutureCallback = uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureCompleteRustBuffer>::toJs(rt, callInvoker, rs_uniffiFutureCallback);
        auto js_uniffiCallbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiCallbackData);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_sourceId, js_uniffiFutureCallback, js_uniffiCallbackData
            );




            // return type is Struct("ForeignFutureDroppedCallbackStruct")
            // Finally, we need to copy the return value back into the Rust pointer.
            *rs_uniffiOutDroppedCallback =
                uniffi::swarmdrop_mobile_core::Bridging<
                    UniffiForeignFutureDroppedCallbackStruct
                >::fromJs(
                    rt, callInvoker, uniffiResult
                );
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceForeignFileAccessMethod0: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback(uint64_t rs_uniffiHandle, RustBuffer rs_sourceId, UniffiForeignFutureCompleteRustBuffer rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_uniffiHandle,
            rs_sourceId,
            rs_uniffiFutureCallback,
            rs_uniffiCallbackData,
            rs_uniffiOutDroppedCallback);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceForeignFileAccessMethod0
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod0::vtablecallbackinterfaceforeignfileaccess
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_sourceId, UniffiForeignFutureCompleteRustBuffer rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_sourceId
                    , rs_uniffiFutureCallback
                    , rs_uniffiCallbackData
                    , rs_uniffiOutDroppedCallback](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_sourceId
                        , rs_uniffiFutureCallback
                        , rs_uniffiCallbackData
                        , rs_uniffiOutDroppedCallback);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod0::vtablecallbackinterfaceforeignfileaccess
    // Implementation of CallbackInterfaceForeignFileAccessMethod1 for vtable field read_source_chunk in VTableCallbackInterfaceForeignFileAccess


// Callback function: uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod1::vtablecallbackinterfaceforeignfileaccess::UniffiCallbackInterfaceForeignFileAccessMethod1
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod1::vtablecallbackinterfaceforeignfileaccess {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, uint64_t, uint64_t, UniffiForeignFutureCompleteRustBuffer, uint64_t, UniffiForeignFutureDroppedCallbackStruct *)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_sourceId
            ,uint64_t rs_offset
            ,uint64_t rs_length
            ,UniffiForeignFutureCompleteRustBuffer rs_uniffiFutureCallback
            ,uint64_t rs_uniffiCallbackData
            ,UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_sourceId = uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_sourceId);
        auto js_offset = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_offset);
        auto js_length = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_length);
        auto js_uniffiFutureCallback = uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureCompleteRustBuffer>::toJs(rt, callInvoker, rs_uniffiFutureCallback);
        auto js_uniffiCallbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiCallbackData);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_sourceId, js_offset, js_length, js_uniffiFutureCallback, js_uniffiCallbackData
            );




            // return type is Struct("ForeignFutureDroppedCallbackStruct")
            // Finally, we need to copy the return value back into the Rust pointer.
            *rs_uniffiOutDroppedCallback =
                uniffi::swarmdrop_mobile_core::Bridging<
                    UniffiForeignFutureDroppedCallbackStruct
                >::fromJs(
                    rt, callInvoker, uniffiResult
                );
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceForeignFileAccessMethod1: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback(uint64_t rs_uniffiHandle, RustBuffer rs_sourceId, uint64_t rs_offset, uint64_t rs_length, UniffiForeignFutureCompleteRustBuffer rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_uniffiHandle,
            rs_sourceId,
            rs_offset,
            rs_length,
            rs_uniffiFutureCallback,
            rs_uniffiCallbackData,
            rs_uniffiOutDroppedCallback);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceForeignFileAccessMethod1
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod1::vtablecallbackinterfaceforeignfileaccess
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_sourceId, uint64_t rs_offset, uint64_t rs_length, UniffiForeignFutureCompleteRustBuffer rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_sourceId
                    , rs_offset
                    , rs_length
                    , rs_uniffiFutureCallback
                    , rs_uniffiCallbackData
                    , rs_uniffiOutDroppedCallback](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_sourceId
                        , rs_offset
                        , rs_length
                        , rs_uniffiFutureCallback
                        , rs_uniffiCallbackData
                        , rs_uniffiOutDroppedCallback);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod1::vtablecallbackinterfaceforeignfileaccess
    // Implementation of CallbackInterfaceForeignFileAccessMethod2 for vtable field create_sink in VTableCallbackInterfaceForeignFileAccess


// Callback function: uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod2::vtablecallbackinterfaceforeignfileaccess::UniffiCallbackInterfaceForeignFileAccessMethod2
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod2::vtablecallbackinterfaceforeignfileaccess {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, UniffiForeignFutureCompleteRustBuffer, uint64_t, UniffiForeignFutureDroppedCallbackStruct *)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_metadata
            ,UniffiForeignFutureCompleteRustBuffer rs_uniffiFutureCallback
            ,uint64_t rs_uniffiCallbackData
            ,UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_metadata = uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_metadata);
        auto js_uniffiFutureCallback = uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureCompleteRustBuffer>::toJs(rt, callInvoker, rs_uniffiFutureCallback);
        auto js_uniffiCallbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiCallbackData);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_metadata, js_uniffiFutureCallback, js_uniffiCallbackData
            );




            // return type is Struct("ForeignFutureDroppedCallbackStruct")
            // Finally, we need to copy the return value back into the Rust pointer.
            *rs_uniffiOutDroppedCallback =
                uniffi::swarmdrop_mobile_core::Bridging<
                    UniffiForeignFutureDroppedCallbackStruct
                >::fromJs(
                    rt, callInvoker, uniffiResult
                );
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceForeignFileAccessMethod2: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback(uint64_t rs_uniffiHandle, RustBuffer rs_metadata, UniffiForeignFutureCompleteRustBuffer rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_uniffiHandle,
            rs_metadata,
            rs_uniffiFutureCallback,
            rs_uniffiCallbackData,
            rs_uniffiOutDroppedCallback);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceForeignFileAccessMethod2
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod2::vtablecallbackinterfaceforeignfileaccess
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_metadata, UniffiForeignFutureCompleteRustBuffer rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_metadata
                    , rs_uniffiFutureCallback
                    , rs_uniffiCallbackData
                    , rs_uniffiOutDroppedCallback](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_metadata
                        , rs_uniffiFutureCallback
                        , rs_uniffiCallbackData
                        , rs_uniffiOutDroppedCallback);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod2::vtablecallbackinterfaceforeignfileaccess
    // Implementation of CallbackInterfaceForeignFileAccessMethod3 for vtable field open_or_create_sink in VTableCallbackInterfaceForeignFileAccess


// Callback function: uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod3::vtablecallbackinterfaceforeignfileaccess::UniffiCallbackInterfaceForeignFileAccessMethod3
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod3::vtablecallbackinterfaceforeignfileaccess {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, UniffiForeignFutureCompleteRustBuffer, uint64_t, UniffiForeignFutureDroppedCallbackStruct *)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_metadata
            ,UniffiForeignFutureCompleteRustBuffer rs_uniffiFutureCallback
            ,uint64_t rs_uniffiCallbackData
            ,UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_metadata = uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_metadata);
        auto js_uniffiFutureCallback = uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureCompleteRustBuffer>::toJs(rt, callInvoker, rs_uniffiFutureCallback);
        auto js_uniffiCallbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiCallbackData);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_metadata, js_uniffiFutureCallback, js_uniffiCallbackData
            );




            // return type is Struct("ForeignFutureDroppedCallbackStruct")
            // Finally, we need to copy the return value back into the Rust pointer.
            *rs_uniffiOutDroppedCallback =
                uniffi::swarmdrop_mobile_core::Bridging<
                    UniffiForeignFutureDroppedCallbackStruct
                >::fromJs(
                    rt, callInvoker, uniffiResult
                );
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceForeignFileAccessMethod3: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback(uint64_t rs_uniffiHandle, RustBuffer rs_metadata, UniffiForeignFutureCompleteRustBuffer rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_uniffiHandle,
            rs_metadata,
            rs_uniffiFutureCallback,
            rs_uniffiCallbackData,
            rs_uniffiOutDroppedCallback);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceForeignFileAccessMethod3
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod3::vtablecallbackinterfaceforeignfileaccess
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_metadata, UniffiForeignFutureCompleteRustBuffer rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_metadata
                    , rs_uniffiFutureCallback
                    , rs_uniffiCallbackData
                    , rs_uniffiOutDroppedCallback](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_metadata
                        , rs_uniffiFutureCallback
                        , rs_uniffiCallbackData
                        , rs_uniffiOutDroppedCallback);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod3::vtablecallbackinterfaceforeignfileaccess
    // Implementation of CallbackInterfaceForeignFileAccessMethod4 for vtable field write_sink_chunk in VTableCallbackInterfaceForeignFileAccess


// Callback function: uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod4::vtablecallbackinterfaceforeignfileaccess::UniffiCallbackInterfaceForeignFileAccessMethod4
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod4::vtablecallbackinterfaceforeignfileaccess {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, uint64_t, RustBuffer, UniffiForeignFutureCompleteVoid, uint64_t, UniffiForeignFutureDroppedCallbackStruct *)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_sinkId
            ,uint64_t rs_offset
            ,RustBuffer rs_data
            ,UniffiForeignFutureCompleteVoid rs_uniffiFutureCallback
            ,uint64_t rs_uniffiCallbackData
            ,UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_sinkId = uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_sinkId);
        auto js_offset = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_offset);
        auto js_data = uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_data);
        auto js_uniffiFutureCallback = uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureCompleteVoid>::toJs(rt, callInvoker, rs_uniffiFutureCallback);
        auto js_uniffiCallbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiCallbackData);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_sinkId, js_offset, js_data, js_uniffiFutureCallback, js_uniffiCallbackData
            );




            // return type is Struct("ForeignFutureDroppedCallbackStruct")
            // Finally, we need to copy the return value back into the Rust pointer.
            *rs_uniffiOutDroppedCallback =
                uniffi::swarmdrop_mobile_core::Bridging<
                    UniffiForeignFutureDroppedCallbackStruct
                >::fromJs(
                    rt, callInvoker, uniffiResult
                );
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceForeignFileAccessMethod4: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback(uint64_t rs_uniffiHandle, RustBuffer rs_sinkId, uint64_t rs_offset, RustBuffer rs_data, UniffiForeignFutureCompleteVoid rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_uniffiHandle,
            rs_sinkId,
            rs_offset,
            rs_data,
            rs_uniffiFutureCallback,
            rs_uniffiCallbackData,
            rs_uniffiOutDroppedCallback);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceForeignFileAccessMethod4
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod4::vtablecallbackinterfaceforeignfileaccess
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_sinkId, uint64_t rs_offset, RustBuffer rs_data, UniffiForeignFutureCompleteVoid rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_sinkId
                    , rs_offset
                    , rs_data
                    , rs_uniffiFutureCallback
                    , rs_uniffiCallbackData
                    , rs_uniffiOutDroppedCallback](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_sinkId
                        , rs_offset
                        , rs_data
                        , rs_uniffiFutureCallback
                        , rs_uniffiCallbackData
                        , rs_uniffiOutDroppedCallback);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod4::vtablecallbackinterfaceforeignfileaccess
    // Implementation of CallbackInterfaceForeignFileAccessMethod5 for vtable field finalize_sink in VTableCallbackInterfaceForeignFileAccess


// Callback function: uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod5::vtablecallbackinterfaceforeignfileaccess::UniffiCallbackInterfaceForeignFileAccessMethod5
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod5::vtablecallbackinterfaceforeignfileaccess {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, UniffiForeignFutureCompleteVoid, uint64_t, UniffiForeignFutureDroppedCallbackStruct *)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_sinkId
            ,UniffiForeignFutureCompleteVoid rs_uniffiFutureCallback
            ,uint64_t rs_uniffiCallbackData
            ,UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_sinkId = uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_sinkId);
        auto js_uniffiFutureCallback = uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureCompleteVoid>::toJs(rt, callInvoker, rs_uniffiFutureCallback);
        auto js_uniffiCallbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiCallbackData);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_sinkId, js_uniffiFutureCallback, js_uniffiCallbackData
            );




            // return type is Struct("ForeignFutureDroppedCallbackStruct")
            // Finally, we need to copy the return value back into the Rust pointer.
            *rs_uniffiOutDroppedCallback =
                uniffi::swarmdrop_mobile_core::Bridging<
                    UniffiForeignFutureDroppedCallbackStruct
                >::fromJs(
                    rt, callInvoker, uniffiResult
                );
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceForeignFileAccessMethod5: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback(uint64_t rs_uniffiHandle, RustBuffer rs_sinkId, UniffiForeignFutureCompleteVoid rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_uniffiHandle,
            rs_sinkId,
            rs_uniffiFutureCallback,
            rs_uniffiCallbackData,
            rs_uniffiOutDroppedCallback);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceForeignFileAccessMethod5
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod5::vtablecallbackinterfaceforeignfileaccess
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_sinkId, UniffiForeignFutureCompleteVoid rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_sinkId
                    , rs_uniffiFutureCallback
                    , rs_uniffiCallbackData
                    , rs_uniffiOutDroppedCallback](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_sinkId
                        , rs_uniffiFutureCallback
                        , rs_uniffiCallbackData
                        , rs_uniffiOutDroppedCallback);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod5::vtablecallbackinterfaceforeignfileaccess
    // Implementation of CallbackInterfaceForeignFileAccessMethod6 for vtable field cleanup_sink in VTableCallbackInterfaceForeignFileAccess


// Callback function: uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod6::vtablecallbackinterfaceforeignfileaccess::UniffiCallbackInterfaceForeignFileAccessMethod6
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod6::vtablecallbackinterfaceforeignfileaccess {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, UniffiForeignFutureCompleteVoid, uint64_t, UniffiForeignFutureDroppedCallbackStruct *)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_sinkId
            ,UniffiForeignFutureCompleteVoid rs_uniffiFutureCallback
            ,uint64_t rs_uniffiCallbackData
            ,UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_sinkId = uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_sinkId);
        auto js_uniffiFutureCallback = uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureCompleteVoid>::toJs(rt, callInvoker, rs_uniffiFutureCallback);
        auto js_uniffiCallbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiCallbackData);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_sinkId, js_uniffiFutureCallback, js_uniffiCallbackData
            );




            // return type is Struct("ForeignFutureDroppedCallbackStruct")
            // Finally, we need to copy the return value back into the Rust pointer.
            *rs_uniffiOutDroppedCallback =
                uniffi::swarmdrop_mobile_core::Bridging<
                    UniffiForeignFutureDroppedCallbackStruct
                >::fromJs(
                    rt, callInvoker, uniffiResult
                );
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceForeignFileAccessMethod6: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback(uint64_t rs_uniffiHandle, RustBuffer rs_sinkId, UniffiForeignFutureCompleteVoid rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_uniffiHandle,
            rs_sinkId,
            rs_uniffiFutureCallback,
            rs_uniffiCallbackData,
            rs_uniffiOutDroppedCallback);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceForeignFileAccessMethod6
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod6::vtablecallbackinterfaceforeignfileaccess
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_sinkId, UniffiForeignFutureCompleteVoid rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_sinkId
                    , rs_uniffiFutureCallback
                    , rs_uniffiCallbackData
                    , rs_uniffiOutDroppedCallback](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_sinkId
                        , rs_uniffiFutureCallback
                        , rs_uniffiCallbackData
                        , rs_uniffiOutDroppedCallback);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod6::vtablecallbackinterfaceforeignfileaccess
namespace uniffi::swarmdrop_mobile_core {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiVTableCallbackInterfaceForeignFileAccess> {
  static UniffiVTableCallbackInterfaceForeignFileAccess fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiVTableCallbackInterfaceForeignFileAccess");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiVTableCallbackInterfaceForeignFileAccess rsObject;

    // Create the vtable from the js callbacks.
    rsObject.uniffi_free = uniffi::swarmdrop_mobile_core::st::vtablecallbackinterfaceforeignfileaccess::vtablecallbackinterfaceforeignfileaccess::free::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "uniffiFree")
        );
    rsObject.uniffi_clone = uniffi::swarmdrop_mobile_core::cb::callbackinterfaceclone::vtablecallbackinterfaceforeignfileaccess::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "uniffiClone")
        );
    rsObject.source_metadata = uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod0::vtablecallbackinterfaceforeignfileaccess::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "sourceMetadata")
        );
    rsObject.read_source_chunk = uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod1::vtablecallbackinterfaceforeignfileaccess::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "readSourceChunk")
        );
    rsObject.create_sink = uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod2::vtablecallbackinterfaceforeignfileaccess::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "createSink")
        );
    rsObject.open_or_create_sink = uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod3::vtablecallbackinterfaceforeignfileaccess::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "openOrCreateSink")
        );
    rsObject.write_sink_chunk = uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod4::vtablecallbackinterfaceforeignfileaccess::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "writeSinkChunk")
        );
    rsObject.finalize_sink = uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod5::vtablecallbackinterfaceforeignfileaccess::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "finalizeSink")
        );
    rsObject.cleanup_sink = uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod6::vtablecallbackinterfaceforeignfileaccess::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "cleanupSink")
        );

    return rsObject;
  }
};

} // namespace uniffi::swarmdrop_mobile_core
    // Implementation of CallbackInterfaceClone for vtable field uniffi_clone in VTableCallbackInterfaceForeignKeychainProvider


// Callback function: uniffi::swarmdrop_mobile_core::cb::callbackinterfaceclone::vtablecallbackinterfaceforeignkeychainprovider::UniffiCallbackInterfaceClone
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceclone::vtablecallbackinterfaceforeignkeychainprovider {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, uint64_t*)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_handle
            , uint64_t* uniffi_direct_return) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_handle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_handle);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_handle
            );




            // Write the direct return value back to the caller.
            if (uniffi_direct_return != nullptr) {
                *uniffi_direct_return = uniffi_jsi::Bridging<uint64_t>::fromJs(
                    rt, callInvoker, uniffiResult
                );
            }
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceClone: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static uint64_t callback(uint64_t rs_handle) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return 0;
        }
        uint64_t uniffi_result = 0;

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_handle,
            &uniffi_result);
        return uniffi_result;
    }

    [[maybe_unused]] static UniffiCallbackInterfaceClone
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::cb::callbackinterfaceclone::vtablecallbackinterfaceforeignkeychainprovider
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_handle, uint64_t* uniffi_direct_return) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_handle, uniffi_direct_return](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_handle, uniffi_direct_return);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceclone::vtablecallbackinterfaceforeignkeychainprovider
    // Implementation of CallbackInterfaceForeignKeychainProviderMethod0 for vtable field load_identity in VTableCallbackInterfaceForeignKeychainProvider


// Callback function: uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod0::vtablecallbackinterfaceforeignkeychainprovider::UniffiCallbackInterfaceForeignKeychainProviderMethod0
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod0::vtablecallbackinterfaceforeignkeychainprovider {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, UniffiForeignFutureCompleteRustBuffer, uint64_t, UniffiForeignFutureDroppedCallbackStruct *)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,UniffiForeignFutureCompleteRustBuffer rs_uniffiFutureCallback
            ,uint64_t rs_uniffiCallbackData
            ,UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_uniffiFutureCallback = uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureCompleteRustBuffer>::toJs(rt, callInvoker, rs_uniffiFutureCallback);
        auto js_uniffiCallbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiCallbackData);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_uniffiFutureCallback, js_uniffiCallbackData
            );




            // return type is Struct("ForeignFutureDroppedCallbackStruct")
            // Finally, we need to copy the return value back into the Rust pointer.
            *rs_uniffiOutDroppedCallback =
                uniffi::swarmdrop_mobile_core::Bridging<
                    UniffiForeignFutureDroppedCallbackStruct
                >::fromJs(
                    rt, callInvoker, uniffiResult
                );
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceForeignKeychainProviderMethod0: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback(uint64_t rs_uniffiHandle, UniffiForeignFutureCompleteRustBuffer rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_uniffiHandle,
            rs_uniffiFutureCallback,
            rs_uniffiCallbackData,
            rs_uniffiOutDroppedCallback);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceForeignKeychainProviderMethod0
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod0::vtablecallbackinterfaceforeignkeychainprovider
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_uniffiHandle, UniffiForeignFutureCompleteRustBuffer rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_uniffiFutureCallback
                    , rs_uniffiCallbackData
                    , rs_uniffiOutDroppedCallback](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_uniffiFutureCallback
                        , rs_uniffiCallbackData
                        , rs_uniffiOutDroppedCallback);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod0::vtablecallbackinterfaceforeignkeychainprovider
    // Implementation of CallbackInterfaceForeignKeychainProviderMethod1 for vtable field save_identity in VTableCallbackInterfaceForeignKeychainProvider


// Callback function: uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod1::vtablecallbackinterfaceforeignkeychainprovider::UniffiCallbackInterfaceForeignKeychainProviderMethod1
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod1::vtablecallbackinterfaceforeignkeychainprovider {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, UniffiForeignFutureCompleteVoid, uint64_t, UniffiForeignFutureDroppedCallbackStruct *)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_keypair
            ,UniffiForeignFutureCompleteVoid rs_uniffiFutureCallback
            ,uint64_t rs_uniffiCallbackData
            ,UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_keypair = uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_keypair);
        auto js_uniffiFutureCallback = uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureCompleteVoid>::toJs(rt, callInvoker, rs_uniffiFutureCallback);
        auto js_uniffiCallbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiCallbackData);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_keypair, js_uniffiFutureCallback, js_uniffiCallbackData
            );




            // return type is Struct("ForeignFutureDroppedCallbackStruct")
            // Finally, we need to copy the return value back into the Rust pointer.
            *rs_uniffiOutDroppedCallback =
                uniffi::swarmdrop_mobile_core::Bridging<
                    UniffiForeignFutureDroppedCallbackStruct
                >::fromJs(
                    rt, callInvoker, uniffiResult
                );
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceForeignKeychainProviderMethod1: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback(uint64_t rs_uniffiHandle, RustBuffer rs_keypair, UniffiForeignFutureCompleteVoid rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_uniffiHandle,
            rs_keypair,
            rs_uniffiFutureCallback,
            rs_uniffiCallbackData,
            rs_uniffiOutDroppedCallback);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceForeignKeychainProviderMethod1
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod1::vtablecallbackinterfaceforeignkeychainprovider
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_keypair, UniffiForeignFutureCompleteVoid rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_keypair
                    , rs_uniffiFutureCallback
                    , rs_uniffiCallbackData
                    , rs_uniffiOutDroppedCallback](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_keypair
                        , rs_uniffiFutureCallback
                        , rs_uniffiCallbackData
                        , rs_uniffiOutDroppedCallback);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod1::vtablecallbackinterfaceforeignkeychainprovider
    // Implementation of CallbackInterfaceForeignKeychainProviderMethod2 for vtable field delete_identity in VTableCallbackInterfaceForeignKeychainProvider


// Callback function: uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod2::vtablecallbackinterfaceforeignkeychainprovider::UniffiCallbackInterfaceForeignKeychainProviderMethod2
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod2::vtablecallbackinterfaceforeignkeychainprovider {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, UniffiForeignFutureCompleteVoid, uint64_t, UniffiForeignFutureDroppedCallbackStruct *)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,UniffiForeignFutureCompleteVoid rs_uniffiFutureCallback
            ,uint64_t rs_uniffiCallbackData
            ,UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_uniffiFutureCallback = uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureCompleteVoid>::toJs(rt, callInvoker, rs_uniffiFutureCallback);
        auto js_uniffiCallbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiCallbackData);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_uniffiFutureCallback, js_uniffiCallbackData
            );




            // return type is Struct("ForeignFutureDroppedCallbackStruct")
            // Finally, we need to copy the return value back into the Rust pointer.
            *rs_uniffiOutDroppedCallback =
                uniffi::swarmdrop_mobile_core::Bridging<
                    UniffiForeignFutureDroppedCallbackStruct
                >::fromJs(
                    rt, callInvoker, uniffiResult
                );
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceForeignKeychainProviderMethod2: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback(uint64_t rs_uniffiHandle, UniffiForeignFutureCompleteVoid rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_uniffiHandle,
            rs_uniffiFutureCallback,
            rs_uniffiCallbackData,
            rs_uniffiOutDroppedCallback);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceForeignKeychainProviderMethod2
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod2::vtablecallbackinterfaceforeignkeychainprovider
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_uniffiHandle, UniffiForeignFutureCompleteVoid rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_uniffiFutureCallback
                    , rs_uniffiCallbackData
                    , rs_uniffiOutDroppedCallback](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_uniffiFutureCallback
                        , rs_uniffiCallbackData
                        , rs_uniffiOutDroppedCallback);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod2::vtablecallbackinterfaceforeignkeychainprovider
    // Implementation of CallbackInterfaceForeignKeychainProviderMethod3 for vtable field load_paired_devices_json in VTableCallbackInterfaceForeignKeychainProvider


// Callback function: uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod3::vtablecallbackinterfaceforeignkeychainprovider::UniffiCallbackInterfaceForeignKeychainProviderMethod3
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod3::vtablecallbackinterfaceforeignkeychainprovider {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, UniffiForeignFutureCompleteRustBuffer, uint64_t, UniffiForeignFutureDroppedCallbackStruct *)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,UniffiForeignFutureCompleteRustBuffer rs_uniffiFutureCallback
            ,uint64_t rs_uniffiCallbackData
            ,UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_uniffiFutureCallback = uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureCompleteRustBuffer>::toJs(rt, callInvoker, rs_uniffiFutureCallback);
        auto js_uniffiCallbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiCallbackData);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_uniffiFutureCallback, js_uniffiCallbackData
            );




            // return type is Struct("ForeignFutureDroppedCallbackStruct")
            // Finally, we need to copy the return value back into the Rust pointer.
            *rs_uniffiOutDroppedCallback =
                uniffi::swarmdrop_mobile_core::Bridging<
                    UniffiForeignFutureDroppedCallbackStruct
                >::fromJs(
                    rt, callInvoker, uniffiResult
                );
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceForeignKeychainProviderMethod3: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback(uint64_t rs_uniffiHandle, UniffiForeignFutureCompleteRustBuffer rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_uniffiHandle,
            rs_uniffiFutureCallback,
            rs_uniffiCallbackData,
            rs_uniffiOutDroppedCallback);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceForeignKeychainProviderMethod3
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod3::vtablecallbackinterfaceforeignkeychainprovider
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_uniffiHandle, UniffiForeignFutureCompleteRustBuffer rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_uniffiFutureCallback
                    , rs_uniffiCallbackData
                    , rs_uniffiOutDroppedCallback](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_uniffiFutureCallback
                        , rs_uniffiCallbackData
                        , rs_uniffiOutDroppedCallback);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod3::vtablecallbackinterfaceforeignkeychainprovider
    // Implementation of CallbackInterfaceForeignKeychainProviderMethod4 for vtable field save_paired_devices_json in VTableCallbackInterfaceForeignKeychainProvider


// Callback function: uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod4::vtablecallbackinterfaceforeignkeychainprovider::UniffiCallbackInterfaceForeignKeychainProviderMethod4
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod4::vtablecallbackinterfaceforeignkeychainprovider {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, UniffiForeignFutureCompleteVoid, uint64_t, UniffiForeignFutureDroppedCallbackStruct *)> rsLambda = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_devicesJson
            ,UniffiForeignFutureCompleteVoid rs_uniffiFutureCallback
            ,uint64_t rs_uniffiCallbackData
            ,UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_devicesJson = uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_devicesJson);
        auto js_uniffiFutureCallback = uniffi::swarmdrop_mobile_core::Bridging<UniffiForeignFutureCompleteVoid>::toJs(rt, callInvoker, rs_uniffiFutureCallback);
        auto js_uniffiCallbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiCallbackData);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_devicesJson, js_uniffiFutureCallback, js_uniffiCallbackData
            );




            // return type is Struct("ForeignFutureDroppedCallbackStruct")
            // Finally, we need to copy the return value back into the Rust pointer.
            *rs_uniffiOutDroppedCallback =
                uniffi::swarmdrop_mobile_core::Bridging<
                    UniffiForeignFutureDroppedCallbackStruct
                >::fromJs(
                    rt, callInvoker, uniffiResult
                );
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceForeignKeychainProviderMethod4: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback(uint64_t rs_uniffiHandle, RustBuffer rs_devicesJson, UniffiForeignFutureCompleteVoid rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda.
        //
        // If rsLamda is null, then there is no runtime to call into.
        if (rsLambda == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda(
            rs_uniffiHandle,
            rs_devicesJson,
            rs_uniffiFutureCallback,
            rs_uniffiCallbackData,
            rs_uniffiOutDroppedCallback);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceForeignKeychainProviderMethod4
    makeCallbackFunction( // uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod4::vtablecallbackinterfaceforeignkeychainprovider
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        rsLambda = [&rt, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_devicesJson, UniffiForeignFutureCompleteVoid rs_uniffiFutureCallback, uint64_t rs_uniffiCallbackData, UniffiForeignFutureDroppedCallbackStruct * rs_uniffiOutDroppedCallback) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_devicesJson
                    , rs_uniffiFutureCallback
                    , rs_uniffiCallbackData
                    , rs_uniffiOutDroppedCallback](jsi::Runtime &rt) mutable {
                    body(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_devicesJson
                        , rs_uniffiFutureCallback
                        , rs_uniffiCallbackData
                        , rs_uniffiOutDroppedCallback);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(rt, jsLambda);
        };
        return callback;
    }

    // This method is called from the destructor of NativeSwarmdropMobileCore, which only happens
    // when the jsi::Runtime is being destroyed.
    static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda = nullptr;
    }
} // namespace uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod4::vtablecallbackinterfaceforeignkeychainprovider
namespace uniffi::swarmdrop_mobile_core {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiVTableCallbackInterfaceForeignKeychainProvider> {
  static UniffiVTableCallbackInterfaceForeignKeychainProvider fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiVTableCallbackInterfaceForeignKeychainProvider");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiVTableCallbackInterfaceForeignKeychainProvider rsObject;

    // Create the vtable from the js callbacks.
    rsObject.uniffi_free = uniffi::swarmdrop_mobile_core::st::vtablecallbackinterfaceforeignkeychainprovider::vtablecallbackinterfaceforeignkeychainprovider::free::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "uniffiFree")
        );
    rsObject.uniffi_clone = uniffi::swarmdrop_mobile_core::cb::callbackinterfaceclone::vtablecallbackinterfaceforeignkeychainprovider::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "uniffiClone")
        );
    rsObject.load_identity = uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod0::vtablecallbackinterfaceforeignkeychainprovider::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "loadIdentity")
        );
    rsObject.save_identity = uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod1::vtablecallbackinterfaceforeignkeychainprovider::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "saveIdentity")
        );
    rsObject.delete_identity = uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod2::vtablecallbackinterfaceforeignkeychainprovider::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "deleteIdentity")
        );
    rsObject.load_paired_devices_json = uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod3::vtablecallbackinterfaceforeignkeychainprovider::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "loadPairedDevicesJson")
        );
    rsObject.save_paired_devices_json = uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod4::vtablecallbackinterfaceforeignkeychainprovider::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "savePairedDevicesJson")
        );

    return rsObject;
  }
};

} // namespace uniffi::swarmdrop_mobile_core


namespace uniffi::swarmdrop_mobile_core {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiRustFutureContinuationCallback> {
  static UniffiRustFutureContinuationCallback fromJs(
    jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &value
  ) {
    try {
      return uniffi::swarmdrop_mobile_core::cb::rustfuturecontinuationcallback::makeCallbackFunction(
        rt,
        callInvoker,
        value
      );
    } catch (const std::logic_error &e) {
      throw jsi::JSError(rt, e.what());
    }
  }
};

} // namespace uniffi::swarmdrop_mobile_core

NativeSwarmdropMobileCore::NativeSwarmdropMobileCore(
    jsi::Runtime &rt,
    std::shared_ptr<uniffi_runtime::UniffiCallInvoker> invoker
) : callInvoker(invoker), props() {
    // Map from Javascript names to the cpp names
    props["ubrn_uniffi_internal_fn_func_ffi__string_to_byte_length"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_internal_fn_func_ffi__string_to_byte_length"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_internal_fn_func_ffi__string_to_byte_length(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_internal_fn_func_ffi__string_to_arraybuffer"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_internal_fn_func_ffi__string_to_arraybuffer"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_internal_fn_func_ffi__string_to_arraybuffer(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_internal_fn_func_ffi__arraybuffer_to_string"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_internal_fn_func_ffi__arraybuffer_to_string"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_internal_fn_func_ffi__arraybuffer_to_string(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_clone_mobilecore"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_clone_mobilecore"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_clone_mobilecore(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_free_mobilecore"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_free_mobilecore"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_free_mobilecore(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_constructor_mobilecore_new"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_constructor_mobilecore_new"),
        4,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_constructor_mobilecore_new(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_take_last_panic"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_take_last_panic"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_take_last_panic(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_list_devices"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_list_devices"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_list_devices(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_list_paired_devices"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_list_paired_devices"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_list_paired_devices(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_clear_transfer_activity"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_clear_transfer_activity"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_clear_transfer_activity(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_delete_transfer_record"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_delete_transfer_record"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_delete_transfer_record(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_get_transfer_projection"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_get_transfer_projection"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_get_transfer_projection(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_get_transfer_projections"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_get_transfer_projections"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_get_transfer_projections(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_resume_transfer"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_resume_transfer"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_resume_transfer(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_initialize_identity"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_initialize_identity"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_initialize_identity(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_archive_inbox_item"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_archive_inbox_item"),
        3,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_archive_inbox_item(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_delete_inbox_item_record"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_delete_inbox_item_record"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_delete_inbox_item_record(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_get_inbox_item"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_get_inbox_item"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_get_inbox_item(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_list_inbox_items"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_list_inbox_items"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_list_inbox_items(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_mark_inbox_file_missing"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_mark_inbox_file_missing"),
        4,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_mark_inbox_file_missing(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_mark_inbox_item_opened"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_mark_inbox_item_opened"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_mark_inbox_item_opened(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_repair_missing_inbox_items"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_repair_missing_inbox_items"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_repair_missing_inbox_items(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_network_status"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_network_status"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_network_status(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_shutdown_node"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_shutdown_node"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_shutdown_node(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_start_node"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_start_node"),
        3,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_start_node(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_generate_pairing_code"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_generate_pairing_code"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_generate_pairing_code(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_lookup_device_by_code"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_lookup_device_by_code"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_lookup_device_by_code(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_request_pairing"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_request_pairing"),
        4,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_request_pairing(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_respond_pairing_request"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_respond_pairing_request"),
        4,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_respond_pairing_request(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_accept_receive"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_accept_receive"),
        3,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_accept_receive(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_cancel_transfer"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_cancel_transfer"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_cancel_transfer(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_pause_transfer"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_pause_transfer"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_pause_transfer(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_prepare_send"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_prepare_send"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_prepare_send(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_reject_receive"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_reject_receive"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_reject_receive(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_send_prepared"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_send_prepared"),
        5,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_send_prepared(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_clone_foreigneventbus"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_clone_foreigneventbus"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_clone_foreigneventbus(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_free_foreigneventbus"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_free_foreigneventbus"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_free_foreigneventbus(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_init_callback_vtable_foreigneventbus"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_init_callback_vtable_foreigneventbus"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_init_callback_vtable_foreigneventbus(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreigneventbus_emit"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreigneventbus_emit"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_foreigneventbus_emit(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_clone_foreignfileaccess"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_clone_foreignfileaccess"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_clone_foreignfileaccess(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_free_foreignfileaccess"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_free_foreignfileaccess"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_free_foreignfileaccess(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_init_callback_vtable_foreignfileaccess"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_init_callback_vtable_foreignfileaccess"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_init_callback_vtable_foreignfileaccess(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_source_metadata"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_source_metadata"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_source_metadata(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_read_source_chunk"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_read_source_chunk"),
        4,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_read_source_chunk(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_create_sink"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_create_sink"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_create_sink(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_open_or_create_sink"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_open_or_create_sink"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_open_or_create_sink(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_write_sink_chunk"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_write_sink_chunk"),
        4,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_write_sink_chunk(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_finalize_sink"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_finalize_sink"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_finalize_sink(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_cleanup_sink"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_cleanup_sink"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_cleanup_sink(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_clone_foreignkeychainprovider"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_clone_foreignkeychainprovider"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_clone_foreignkeychainprovider(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_free_foreignkeychainprovider"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_free_foreignkeychainprovider"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_free_foreignkeychainprovider(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_init_callback_vtable_foreignkeychainprovider"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_init_callback_vtable_foreignkeychainprovider"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_init_callback_vtable_foreignkeychainprovider(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_load_identity"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_load_identity"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_load_identity(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_save_identity"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_save_identity"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_save_identity(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_delete_identity"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_delete_identity"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_delete_identity(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_load_paired_devices_json"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_load_paired_devices_json"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_load_paired_devices_json(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_save_paired_devices_json"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_save_paired_devices_json"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_save_paired_devices_json(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_u8"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_u8"),
        3,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_poll_u8(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_u8"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_u8"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_u8(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_free_u8"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_free_u8"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_free_u8(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_u8"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_u8"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_complete_u8(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_i8"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_i8"),
        3,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_poll_i8(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_i8"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_i8"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_i8(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_free_i8"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_free_i8"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_free_i8(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_i8"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_i8"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_complete_i8(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_u16"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_u16"),
        3,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_poll_u16(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_u16"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_u16"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_u16(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_free_u16"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_free_u16"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_free_u16(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_u16"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_u16"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_complete_u16(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_i16"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_i16"),
        3,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_poll_i16(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_i16"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_i16"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_i16(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_free_i16"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_free_i16"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_free_i16(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_i16"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_i16"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_complete_i16(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_u32"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_u32"),
        3,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_poll_u32(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_u32"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_u32"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_u32(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_free_u32"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_free_u32"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_free_u32(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_u32"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_u32"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_complete_u32(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_i32"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_i32"),
        3,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_poll_i32(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_i32"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_i32"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_i32(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_free_i32"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_free_i32"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_free_i32(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_i32"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_i32"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_complete_i32(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_u64"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_u64"),
        3,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_poll_u64(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_u64"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_u64"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_u64(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_free_u64"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_free_u64"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_free_u64(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_u64"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_u64"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_complete_u64(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_i64"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_i64"),
        3,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_poll_i64(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_i64"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_i64"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_i64(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_free_i64"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_free_i64"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_free_i64(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_i64"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_i64"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_complete_i64(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_f32"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_f32"),
        3,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_poll_f32(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_f32"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_f32"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_f32(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_free_f32"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_free_f32"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_free_f32(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_f32"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_f32"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_complete_f32(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_f64"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_f64"),
        3,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_poll_f64(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_f64"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_f64"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_f64(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_free_f64"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_free_f64"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_free_f64(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_f64"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_f64"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_complete_f64(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_rust_buffer"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_rust_buffer"),
        3,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_poll_rust_buffer(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_rust_buffer"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_rust_buffer"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_rust_buffer(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_free_rust_buffer"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_free_rust_buffer"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_free_rust_buffer(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_rust_buffer"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_rust_buffer"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_complete_rust_buffer(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_void"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_poll_void"),
        3,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_poll_void(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_void"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_cancel_void"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_void(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_free_void"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_free_void"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_free_void(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_void"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_rust_future_complete_void"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_rust_future_complete_void(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_take_last_panic"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_take_last_panic"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_take_last_panic(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_list_devices"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_list_devices"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_list_devices(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_list_paired_devices"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_list_paired_devices"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_list_paired_devices(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_clear_transfer_activity"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_clear_transfer_activity"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_clear_transfer_activity(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_delete_transfer_record"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_delete_transfer_record"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_delete_transfer_record(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_get_transfer_projection"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_get_transfer_projection"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_get_transfer_projection(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_get_transfer_projections"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_get_transfer_projections"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_get_transfer_projections(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_resume_transfer"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_resume_transfer"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_resume_transfer(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_initialize_identity"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_initialize_identity"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_initialize_identity(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_archive_inbox_item"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_archive_inbox_item"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_archive_inbox_item(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_delete_inbox_item_record"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_delete_inbox_item_record"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_delete_inbox_item_record(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_get_inbox_item"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_get_inbox_item"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_get_inbox_item(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_list_inbox_items"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_list_inbox_items"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_list_inbox_items(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_mark_inbox_file_missing"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_mark_inbox_file_missing"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_mark_inbox_file_missing(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_mark_inbox_item_opened"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_mark_inbox_item_opened"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_mark_inbox_item_opened(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_repair_missing_inbox_items"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_repair_missing_inbox_items"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_repair_missing_inbox_items(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_network_status"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_network_status"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_network_status(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_shutdown_node"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_shutdown_node"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_shutdown_node(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_start_node"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_start_node"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_start_node(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_generate_pairing_code"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_generate_pairing_code"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_generate_pairing_code(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_lookup_device_by_code"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_lookup_device_by_code"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_lookup_device_by_code(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_request_pairing"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_request_pairing"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_request_pairing(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_respond_pairing_request"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_respond_pairing_request"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_respond_pairing_request(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_accept_receive"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_accept_receive"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_accept_receive(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_cancel_transfer"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_cancel_transfer"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_cancel_transfer(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_pause_transfer"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_pause_transfer"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_pause_transfer(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_prepare_send"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_prepare_send"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_prepare_send(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_reject_receive"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_reject_receive"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_reject_receive(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_send_prepared"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_send_prepared"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_send_prepared(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreigneventbus_emit"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreigneventbus_emit"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreigneventbus_emit(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_source_metadata"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_source_metadata"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_source_metadata(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_read_source_chunk"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_read_source_chunk"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_read_source_chunk(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_create_sink"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_create_sink"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_create_sink(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_open_or_create_sink"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_open_or_create_sink"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_open_or_create_sink(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_write_sink_chunk"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_write_sink_chunk"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_write_sink_chunk(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_finalize_sink"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_finalize_sink"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_finalize_sink(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_cleanup_sink"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_cleanup_sink"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_cleanup_sink(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_load_identity"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_load_identity"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_load_identity(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_save_identity"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_save_identity"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_save_identity(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_delete_identity"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_delete_identity"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_delete_identity(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_load_paired_devices_json"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_load_paired_devices_json"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_load_paired_devices_json(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_save_paired_devices_json"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_save_paired_devices_json"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_save_paired_devices_json(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_swarmdrop_mobile_core_checksum_constructor_mobilecore_new"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_swarmdrop_mobile_core_checksum_constructor_mobilecore_new"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_swarmdrop_mobile_core_checksum_constructor_mobilecore_new(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_swarmdrop_mobile_core_uniffi_contract_version"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_swarmdrop_mobile_core_uniffi_contract_version"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_swarmdrop_mobile_core_uniffi_contract_version(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_internal_fn_method_mobilecore_ffi__bless_pointer"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_internal_fn_method_mobilecore_ffi__bless_pointer"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_internal_fn_method_mobilecore_ffi__bless_pointer(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_internal_fn_method_foreigneventbus_ffi__bless_pointer"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_internal_fn_method_foreigneventbus_ffi__bless_pointer"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_internal_fn_method_foreigneventbus_ffi__bless_pointer(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_internal_fn_method_foreignfileaccess_ffi__bless_pointer"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_internal_fn_method_foreignfileaccess_ffi__bless_pointer"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_internal_fn_method_foreignfileaccess_ffi__bless_pointer(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_internal_fn_method_foreignkeychainprovider_ffi__bless_pointer"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_internal_fn_method_foreignkeychainprovider_ffi__bless_pointer"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_internal_fn_method_foreignkeychainprovider_ffi__bless_pointer(rt, thisVal, args, count);
        }
    );
}

void NativeSwarmdropMobileCore::registerModule(jsi::Runtime &rt, std::shared_ptr<react::CallInvoker> callInvoker) {
    auto invoker = std::make_shared<uniffi_runtime::UniffiCallInvoker>(callInvoker);
    auto tm = std::make_shared<NativeSwarmdropMobileCore>(rt, invoker);
    auto obj = rt.global().createFromHostObject(rt, tm);
    rt.global().setProperty(rt, "NativeSwarmdropMobileCore", obj);
}

void NativeSwarmdropMobileCore::unregisterModule(jsi::Runtime &rt) {
    uniffi::swarmdrop_mobile_core::registry::clearRegistry();
}

jsi::Value NativeSwarmdropMobileCore::get(jsi::Runtime& rt, const jsi::PropNameID& name) {
    try {
        return jsi::Value(rt, props.at(name.utf8(rt)));
    }
    catch (std::out_of_range &e) {
        return jsi::Value::undefined();
    }
}

std::vector<jsi::PropNameID> NativeSwarmdropMobileCore::getPropertyNames(jsi::Runtime& rt) {
    std::vector<jsi::PropNameID> rval;
    for (auto& [key, value] : props) {
        rval.push_back(jsi::PropNameID::forUtf8(rt, key));
    }
    return rval;
}

void NativeSwarmdropMobileCore::set(jsi::Runtime& rt, const jsi::PropNameID& name, const jsi::Value& value) {
    props.insert_or_assign(name.utf8(rt), &value);
}

NativeSwarmdropMobileCore::~NativeSwarmdropMobileCore() {
    // Cleanup for callback function RustFutureContinuationCallback
uniffi::swarmdrop_mobile_core::cb::rustfuturecontinuationcallback::cleanup();
    // Cleanup for callback function ForeignFutureDroppedCallback
uniffi::swarmdrop_mobile_core::cb::foreignfuturedroppedcallback::cleanup();
    // Cleanup for "free" callback function CallbackInterfaceFree
uniffi::swarmdrop_mobile_core::st::vtablecallbackinterfaceforeigneventbus::vtablecallbackinterfaceforeigneventbus::free::cleanup();uniffi::swarmdrop_mobile_core::st::vtablecallbackinterfaceforeignfileaccess::vtablecallbackinterfaceforeignfileaccess::free::cleanup();uniffi::swarmdrop_mobile_core::st::vtablecallbackinterfaceforeignkeychainprovider::vtablecallbackinterfaceforeignkeychainprovider::free::cleanup();
uniffi::swarmdrop_mobile_core::cb::callbackinterfaceclone::vtablecallbackinterfaceforeigneventbus::cleanup();
uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeigneventbusmethod0::vtablecallbackinterfaceforeigneventbus::cleanup();
uniffi::swarmdrop_mobile_core::cb::callbackinterfaceclone::vtablecallbackinterfaceforeignfileaccess::cleanup();
uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod0::vtablecallbackinterfaceforeignfileaccess::cleanup();
uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod1::vtablecallbackinterfaceforeignfileaccess::cleanup();
uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod2::vtablecallbackinterfaceforeignfileaccess::cleanup();
uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod3::vtablecallbackinterfaceforeignfileaccess::cleanup();
uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod4::vtablecallbackinterfaceforeignfileaccess::cleanup();
uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod5::vtablecallbackinterfaceforeignfileaccess::cleanup();
uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignfileaccessmethod6::vtablecallbackinterfaceforeignfileaccess::cleanup();
uniffi::swarmdrop_mobile_core::cb::callbackinterfaceclone::vtablecallbackinterfaceforeignkeychainprovider::cleanup();
uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod0::vtablecallbackinterfaceforeignkeychainprovider::cleanup();
uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod1::vtablecallbackinterfaceforeignkeychainprovider::cleanup();
uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod2::vtablecallbackinterfaceforeignkeychainprovider::cleanup();
uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod3::vtablecallbackinterfaceforeignkeychainprovider::cleanup();
uniffi::swarmdrop_mobile_core::cb::callbackinterfaceforeignkeychainprovidermethod4::vtablecallbackinterfaceforeignkeychainprovider::cleanup();
}

// Utility functions for serialization/deserialization of strings.
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_internal_fn_func_ffi__string_to_byte_length(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
    return uniffi_jsi::Bridging<std::string>::string_to_bytelength(rt, args[0]);
}

jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_internal_fn_func_ffi__string_to_arraybuffer(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
    return uniffi_jsi::Bridging<std::string>::string_to_arraybuffer(rt, args[0]);
}

jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_internal_fn_func_ffi__arraybuffer_to_string(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
    return uniffi_jsi::Bridging<std::string>::arraybuffer_to_string(rt, args[0]);
}jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_internal_fn_method_mobilecore_ffi__bless_pointer(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
    auto pointer = uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]);
    auto static destructor = [](uint64_t p) {
        RustCallStatus status = {0};
        uniffi_swarmdrop_mobile_core_fn_free_mobilecore(p, &status);
    };
    auto ptrObj = std::make_shared<uniffi_jsi::DestructibleObject>(pointer, destructor);
    auto obj = jsi::Object::createFromHostObject(rt, ptrObj);
    return jsi::Value(rt, obj);
}jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_internal_fn_method_foreigneventbus_ffi__bless_pointer(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
    auto pointer = uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]);
    auto static destructor = [](uint64_t p) {
        RustCallStatus status = {0};
        uniffi_swarmdrop_mobile_core_fn_free_foreigneventbus(p, &status);
    };
    auto ptrObj = std::make_shared<uniffi_jsi::DestructibleObject>(pointer, destructor);
    auto obj = jsi::Object::createFromHostObject(rt, ptrObj);
    return jsi::Value(rt, obj);
}jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_internal_fn_method_foreignfileaccess_ffi__bless_pointer(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
    auto pointer = uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]);
    auto static destructor = [](uint64_t p) {
        RustCallStatus status = {0};
        uniffi_swarmdrop_mobile_core_fn_free_foreignfileaccess(p, &status);
    };
    auto ptrObj = std::make_shared<uniffi_jsi::DestructibleObject>(pointer, destructor);
    auto obj = jsi::Object::createFromHostObject(rt, ptrObj);
    return jsi::Value(rt, obj);
}jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_internal_fn_method_foreignkeychainprovider_ffi__bless_pointer(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
    auto pointer = uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]);
    auto static destructor = [](uint64_t p) {
        RustCallStatus status = {0};
        uniffi_swarmdrop_mobile_core_fn_free_foreignkeychainprovider(p, &status);
    };
    auto ptrObj = std::make_shared<uniffi_jsi::DestructibleObject>(pointer, destructor);
    auto obj = jsi::Object::createFromHostObject(rt, ptrObj);
    return jsi::Value(rt, obj);
}

// Methods calling directly into the uniffi generated C API of the Rust crate.
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_clone_mobilecore(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_swarmdrop_mobile_core_fn_clone_mobilecore(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_free_mobilecore(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        uniffi_swarmdrop_mobile_core_fn_free_mobilecore(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_constructor_mobilecore_new(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_swarmdrop_mobile_core_fn_constructor_mobilecore_new(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[2]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[3]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_take_last_panic(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_take_last_panic(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_list_devices(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_list_devices(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_list_paired_devices(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_list_paired_devices(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_clear_transfer_activity(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_clear_transfer_activity(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_delete_transfer_record(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_delete_transfer_record(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_get_transfer_projection(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_get_transfer_projection(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_get_transfer_projections(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_get_transfer_projections(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_resume_transfer(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_resume_transfer(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_initialize_identity(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_initialize_identity(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_archive_inbox_item(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_archive_inbox_item(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging<int8_t>::fromJs(rt, callInvoker, args[2])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_delete_inbox_item_record(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_delete_inbox_item_record(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_get_inbox_item(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_get_inbox_item(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_list_inbox_items(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_list_inbox_items(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi_jsi::Bridging<int8_t>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_mark_inbox_file_missing(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_mark_inbox_file_missing(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging<uint32_t>::fromJs(rt, callInvoker, args[2]), uniffi_jsi::Bridging<int8_t>::fromJs(rt, callInvoker, args[3])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_mark_inbox_item_opened(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_mark_inbox_item_opened(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_repair_missing_inbox_items(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_repair_missing_inbox_items(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_network_status(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_network_status(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_shutdown_node(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_shutdown_node(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_start_node(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_start_node(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[2])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_generate_pairing_code(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_generate_pairing_code(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_lookup_device_by_code(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_lookup_device_by_code(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_request_pairing(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_request_pairing(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[2]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[3])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_respond_pairing_request(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_respond_pairing_request(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[1]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[2]), uniffi_jsi::Bridging<int8_t>::fromJs(rt, callInvoker, args[3])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_accept_receive(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_accept_receive(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[2])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_cancel_transfer(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_cancel_transfer(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_pause_transfer(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_pause_transfer(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_prepare_send(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_prepare_send(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_reject_receive(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_reject_receive(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_mobilecore_send_prepared(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_mobilecore_send_prepared(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[2]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[3]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[4])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_clone_foreigneventbus(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_swarmdrop_mobile_core_fn_clone_foreigneventbus(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_free_foreigneventbus(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        uniffi_swarmdrop_mobile_core_fn_free_foreigneventbus(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_init_callback_vtable_foreigneventbus(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
    auto vtableInstance =
        uniffi::swarmdrop_mobile_core::Bridging<UniffiVTableCallbackInterfaceForeignEventBus>::fromJs(
            rt,
            callInvoker,
            args[0]
        );

    std::lock_guard<std::mutex> lock(uniffi::swarmdrop_mobile_core::registry::vtableMutex);
    uniffi_swarmdrop_mobile_core_fn_init_callback_vtable_foreigneventbus(
        uniffi::swarmdrop_mobile_core::registry::putTable(
            "UniffiVTableCallbackInterfaceForeignEventBus",
            vtableInstance
        )
    );
    return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_foreigneventbus_emit(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        uniffi_swarmdrop_mobile_core_fn_method_foreigneventbus_emit(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_clone_foreignfileaccess(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_swarmdrop_mobile_core_fn_clone_foreignfileaccess(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_free_foreignfileaccess(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        uniffi_swarmdrop_mobile_core_fn_free_foreignfileaccess(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_init_callback_vtable_foreignfileaccess(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
    auto vtableInstance =
        uniffi::swarmdrop_mobile_core::Bridging<UniffiVTableCallbackInterfaceForeignFileAccess>::fromJs(
            rt,
            callInvoker,
            args[0]
        );

    std::lock_guard<std::mutex> lock(uniffi::swarmdrop_mobile_core::registry::vtableMutex);
    uniffi_swarmdrop_mobile_core_fn_init_callback_vtable_foreignfileaccess(
        uniffi::swarmdrop_mobile_core::registry::putTable(
            "UniffiVTableCallbackInterfaceForeignFileAccess",
            vtableInstance
        )
    );
    return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_source_metadata(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_source_metadata(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_read_source_chunk(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_read_source_chunk(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[2]), uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[3])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_create_sink(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_create_sink(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_open_or_create_sink(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_open_or_create_sink(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_write_sink_chunk(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_write_sink_chunk(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[2]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[3])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_finalize_sink(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_finalize_sink(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_cleanup_sink(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_foreignfileaccess_cleanup_sink(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_clone_foreignkeychainprovider(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_swarmdrop_mobile_core_fn_clone_foreignkeychainprovider(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_free_foreignkeychainprovider(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        uniffi_swarmdrop_mobile_core_fn_free_foreignkeychainprovider(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_init_callback_vtable_foreignkeychainprovider(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
    auto vtableInstance =
        uniffi::swarmdrop_mobile_core::Bridging<UniffiVTableCallbackInterfaceForeignKeychainProvider>::fromJs(
            rt,
            callInvoker,
            args[0]
        );

    std::lock_guard<std::mutex> lock(uniffi::swarmdrop_mobile_core::registry::vtableMutex);
    uniffi_swarmdrop_mobile_core_fn_init_callback_vtable_foreignkeychainprovider(
        uniffi::swarmdrop_mobile_core::registry::putTable(
            "UniffiVTableCallbackInterfaceForeignKeychainProvider",
            vtableInstance
        )
    );
    return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_load_identity(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_load_identity(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_save_identity(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_save_identity(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_delete_identity(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_delete_identity(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_load_paired_devices_json(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_load_paired_devices_json(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_save_paired_devices_json(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_fn_method_foreignkeychainprovider_save_paired_devices_json(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1])
        );


        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_poll_u8(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_poll_u8(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiRustFutureContinuationCallback>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[2])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_u8(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_cancel_u8(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_free_u8(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_free_u8(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_complete_u8(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = ffi_swarmdrop_mobile_core_rust_future_complete_u8(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return uniffi_jsi::Bridging<uint8_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_poll_i8(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_poll_i8(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiRustFutureContinuationCallback>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[2])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_i8(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_cancel_i8(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_free_i8(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_free_i8(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_complete_i8(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = ffi_swarmdrop_mobile_core_rust_future_complete_i8(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return uniffi_jsi::Bridging<int8_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_poll_u16(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_poll_u16(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiRustFutureContinuationCallback>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[2])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_u16(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_cancel_u16(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_free_u16(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_free_u16(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_complete_u16(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = ffi_swarmdrop_mobile_core_rust_future_complete_u16(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_poll_i16(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_poll_i16(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiRustFutureContinuationCallback>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[2])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_i16(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_cancel_i16(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_free_i16(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_free_i16(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_complete_i16(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = ffi_swarmdrop_mobile_core_rust_future_complete_i16(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return uniffi_jsi::Bridging<int16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_poll_u32(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_poll_u32(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiRustFutureContinuationCallback>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[2])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_u32(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_cancel_u32(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_free_u32(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_free_u32(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_complete_u32(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = ffi_swarmdrop_mobile_core_rust_future_complete_u32(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return uniffi_jsi::Bridging<uint32_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_poll_i32(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_poll_i32(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiRustFutureContinuationCallback>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[2])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_i32(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_cancel_i32(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_free_i32(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_free_i32(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_complete_i32(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = ffi_swarmdrop_mobile_core_rust_future_complete_i32(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return uniffi_jsi::Bridging<int32_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_poll_u64(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_poll_u64(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiRustFutureContinuationCallback>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[2])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_u64(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_cancel_u64(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_free_u64(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_free_u64(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_complete_u64(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = ffi_swarmdrop_mobile_core_rust_future_complete_u64(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_poll_i64(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_poll_i64(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiRustFutureContinuationCallback>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[2])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_i64(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_cancel_i64(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_free_i64(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_free_i64(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_complete_i64(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = ffi_swarmdrop_mobile_core_rust_future_complete_i64(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return uniffi_jsi::Bridging<int64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_poll_f32(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_poll_f32(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiRustFutureContinuationCallback>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[2])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_f32(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_cancel_f32(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_free_f32(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_free_f32(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_complete_f32(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = ffi_swarmdrop_mobile_core_rust_future_complete_f32(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return uniffi_jsi::Bridging<float>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_poll_f64(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_poll_f64(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiRustFutureContinuationCallback>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[2])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_f64(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_cancel_f64(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_free_f64(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_free_f64(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_complete_f64(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = ffi_swarmdrop_mobile_core_rust_future_complete_f64(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return uniffi_jsi::Bridging<double>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_poll_rust_buffer(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_poll_rust_buffer(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiRustFutureContinuationCallback>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[2])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_rust_buffer(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_cancel_rust_buffer(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_free_rust_buffer(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_free_rust_buffer(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_complete_rust_buffer(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = ffi_swarmdrop_mobile_core_rust_future_complete_rust_buffer(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return uniffi::swarmdrop_mobile_core::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_poll_void(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_poll_void(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::swarmdrop_mobile_core::Bridging<UniffiRustFutureContinuationCallback>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[2])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_cancel_void(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_cancel_void(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_free_void(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        ffi_swarmdrop_mobile_core_rust_future_free_void(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0])
        );


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_rust_future_complete_void(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::rustSuccess(rt);
        ffi_swarmdrop_mobile_core_rust_future_complete_void(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]),
            &status
        );
        uniffi::swarmdrop_mobile_core::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);


        return jsi::Value::undefined();
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_take_last_panic(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_take_last_panic(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_list_devices(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_list_devices(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_list_paired_devices(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_list_paired_devices(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_clear_transfer_activity(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_clear_transfer_activity(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_delete_transfer_record(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_delete_transfer_record(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_get_transfer_projection(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_get_transfer_projection(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_get_transfer_projections(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_get_transfer_projections(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_resume_transfer(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_resume_transfer(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_initialize_identity(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_initialize_identity(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_archive_inbox_item(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_archive_inbox_item(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_delete_inbox_item_record(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_delete_inbox_item_record(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_get_inbox_item(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_get_inbox_item(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_list_inbox_items(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_list_inbox_items(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_mark_inbox_file_missing(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_mark_inbox_file_missing(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_mark_inbox_item_opened(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_mark_inbox_item_opened(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_repair_missing_inbox_items(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_repair_missing_inbox_items(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_network_status(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_network_status(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_shutdown_node(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_shutdown_node(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_start_node(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_start_node(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_generate_pairing_code(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_generate_pairing_code(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_lookup_device_by_code(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_lookup_device_by_code(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_request_pairing(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_request_pairing(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_respond_pairing_request(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_respond_pairing_request(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_accept_receive(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_accept_receive(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_cancel_transfer(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_cancel_transfer(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_pause_transfer(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_pause_transfer(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_prepare_send(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_prepare_send(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_reject_receive(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_reject_receive(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_send_prepared(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_mobilecore_send_prepared(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreigneventbus_emit(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_foreigneventbus_emit(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_source_metadata(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_source_metadata(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_read_source_chunk(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_read_source_chunk(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_create_sink(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_create_sink(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_open_or_create_sink(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_open_or_create_sink(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_write_sink_chunk(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_write_sink_chunk(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_finalize_sink(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_finalize_sink(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_cleanup_sink(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_foreignfileaccess_cleanup_sink(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_load_identity(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_load_identity(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_save_identity(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_save_identity(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_delete_identity(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_delete_identity(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_load_paired_devices_json(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_load_paired_devices_json(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_save_paired_devices_json(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_method_foreignkeychainprovider_save_paired_devices_json(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_uniffi_swarmdrop_mobile_core_checksum_constructor_mobilecore_new(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_swarmdrop_mobile_core_checksum_constructor_mobilecore_new(
        );


        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeSwarmdropMobileCore::cpp_ffi_swarmdrop_mobile_core_uniffi_contract_version(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = ffi_swarmdrop_mobile_core_uniffi_contract_version(
        );


        return uniffi_jsi::Bridging<uint32_t>::toJs(rt, callInvoker, value);
}